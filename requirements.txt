fastapi>=0.100
uvicorn>=0.20
# websockets: uvicorn WebSocket 升级依赖（缺则 /v1/logs/ws 全 404）
websockets>=12.0,<14
httpx>=0.24
pydantic>=2.0
pydantic-settings>=2.0
aiosqlite>=0.20.0
# cryptography: fal.ai Kasada x-is-human 纯算签名（AES-256-GCM + PBKDF2-HMAC-SHA256）
cryptography>=41.0
# patchright: fal.ai 会话引导（反检测 Playwright fork，可选，IF_FALAI_ENABLED=1 时需安装）
# patchright>=1.50
# OpenTelemetry（可选：IF_OTEL_ENABLED=1 时需安装）
opentelemetry-api>=1.20,<2.0
opentelemetry-sdk>=1.20,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.20,<2.0
opentelemetry-instrumentation>=0.40b0
opentelemetry-instrumentation-fastapi>=0.40b0
opentelemetry-instrumentation-httpx>=0.40b0
opentelemetry-instrumentation-logging>=0.40b0
prometheus-client>=0.21
# pytest-timeout：单测兜底超时，防真实网络类用例卡死 CI（deploy.yml / ci.yml 均依赖）
pytest-timeout>=2.3

# v18 P1-2: PPT 可编辑产物生成（OOXML 渲染，pip install python-pptx）
python-pptx>=1.0.0
