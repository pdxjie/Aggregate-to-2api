# 参考项目深度侦察报告：记忆 / Skills / MCP 三大类（16 项目）

> 侦察基线：主项目 = 「Python 3.11+ / FastAPI / SQLite(aiosqlite) / SSE / 号池+代理池+DAG 智能体编排 / 记忆 L0-L3 雏形 / skills 加载器已有 / 付费上游预算=0 红线」
> 侦察时间：2026-09-11。本文件为主项目根目录 `.ref-scout-*.md` 系列第四份（子代理执行时未落预期文件名，由主会话据其完整输出补写）。
> 说明：补写版为**关键结论浓缩**，完整证据（`路径:行号` 逐项）见下文各节；所有引用均来自子代理实际读文件输出，未加新内容。

---

## 任务 C：记忆类（6 项目）

### letta / letta-code
- memfs 目录协议：`~/.letta/agents/<agentId>/memory/` + 每目录 MUST 有 `MEMORY.md` 索引 + `system/` core memory + 根级 detached
- `letta-code/src/agent/memory-format.ts:11-46` `isCoreMemoryPath`；`:58-75` `isProjectedMemoryPath`
- 格式双版本迁移检测 `detectMemoryFormat:14-18`
- scoped 解析优先级 `memory-filesystem.ts:120-148`
- 写前索引校验 `assertMemfsV2MemoryPathIndexed:78-97`（防孤儿文件）
- git-backed 记忆 `memory-filesystem.ts:180-216` + git hooks ✓ 主体不搬（单 agent sticky-note，无多用户/号池语义）

### mem0
- `infer=True` 的 LLM 增量提取（ADD/UPDATE/DELETE 事件）+ Observation Date 唯一时间锚 + 自带 `linked_memory_ids` 去重关联 → `mem0/configs/prompts.py:12-…`
- 身份键不信任 metadata：`_strip_identity_keys`（main.py:95-105）+ `_IDENTITY_KEYS = ENTITY_PARAMS | {"actor_id"}`（main.py:64-66）
- 混合打分规范化 `score_and_rank`（utils/scoring.py:60-135）：语义先过 threshold + BM25 sigmoid + 实体 boost + `explain=True` 输出 score_details
- BM25 参数按查询长度自适应（utils/scoring.py:18-36，3/6/9/15 词分档）
- 过期记忆软隐藏 `_is_expired`（main.py:427-450）✓ 迁移：增量提取 prompt + score_details 可审计化 + 身份键剥离

### agentmemory-main（consolidation-pipeline 重点）
- 合并管线（`consolidation-pipeline.ts:59-150`）：semantic 合并仅 summaries≥5；`<fact confidence>` 标签式 XML 契约 + 正则解析；已有事实只 bump；程序性记忆 `<procedure name trigger>` 仅 pattern≥2
- 版本化 supersedes 链（`consolidate.ts:160-211`）：旧 isLatest=false，新 version+1/parentId/supersedes
- 级联过时（`cascade.ts:12-80`）：被 supersede → sourceObservationIds 交集标 stale → sibling 找共享 concept
- 强度衰变（`consolidation-pipeline.ts:12-33`）：按 lastAccessedAt 每 decayDays 乘 0.9 下限 0.1
- 成本护栏（`consolidate.ts:113-117`）：`MAX_LLM_CALLS = 10` + 30s 超时；importance≥5 才参与 ✓ 三样可 1:1 搬进 SQLite

### TencentDB-Agent-Memory
- L0-L3 四层显式模型（README_CN.md:250-258）+ 生成与召回分层（L2/L3 快速进入语境 + L1/L0 BM25+向量+RRF 回事实）+ 条数/字符/超时预算（:259）
- Skill 也是记忆资产：`MemoryCore/src/core/skill/`（skill-extractor.ts 598 行/head-tail 截断 head 8000/tail 32000，skill-core.ts 774 行/owner 校验 assertOwnerWrap:706-729）
- RRF 融合 `store/search-utils.ts:38-68`（RRF_K=60）✓ 迁移 L0-L3 预算层 + skill 资产化 + RRF 纯函数

### ai-memory
- wiki 真相源 + DB 派生索引（README.md:44-45,306-310）RAW 不可变片段 + git 版本化 + `永远可从文件重建索引`
- admission preflight 先于 LLM（`consolidator.rs:157-163`，拒绝的 scope 不花 token）+ dry-run 不花 LLM + 会话合并 auto-commit（:232-240）
- frontmatter tier + 高阻力 slot 保护（`consolidator.rs:714-790`）✓ 迁移 admission-before-LLM + frontmatter tier

### claude-supermemory
- hook 事件即采集点（SessionStart 注入回忆 / UserPromptSubmit read-directive 5s / PreToolUse 放行只读 / Stop 捕获）
- 3s/4s 超时失败即放行（`lib/api.js`：hooks sit between user and Claude）
- 容器标签稳定化 `normalizeGitRemote` → sha256().slice(0,16)（`lib/container-tag.js:15-45`）
- AGENT_ENTITY_CONTEXT 提取指令（`lib/api.js:1-30` 明确 EXTRACT 架构/约定/SKIP 临时状态）✓ 迁移 3s 超时故障语义 + 容器命名

## 任务 D：Skills 类（6 项目）

### claude-skills-main
- 10 个可复制模式（`SKILL-AUTHORING-STANDARD.md:97-346`）：Context-First / Practitioner Voice / Reference Separation（SKILL.md<500 行）/ Proactive Triggers / Output Artifacts（"当你要求 X→得到 Y"表）/ Python Tools（stdlib-only）
- SKILL.md 模板骨架（:12-96 frontmatter + Before Starting / How This Skill Works / Proactive Triggers / Output Artifacts / Communication / Related Skills）
- 四件套目录（SKILL.md + assets/ + references/ + scripts/）
- 多 Agent 适配（.claude-plugin/plugin.json + .codex/instructions.md + CLAUDE.md）✓ 10 模式全进主项目加载器规范

### book-to-skill
- SKILL.md 自包含多模式路由（SKILL.md:40-63 4 种模式按意图分叉）
- REPL 式大书访问（:248-276）wc -w / grep -n Chapter / sed 切片，>50k token 当语料不整体 Read
- pre-flight 成本预估（:211-247）生成前给 token/价格/文件清单等确认
- 四层输出（SKILL.md + chapters/chNN-* + glossary + patterns + cheatsheet :411-497），SKILL 正文 <4000 token ✓ 文档→技能流水线可直接新增

### SkillX-main（评估：不值得搬）
- 统一 5-key skill schema `Skill{name, document, content, tools, metadata}`（core/skill.py:17-100）
- 功能性技能提取（extraction/skill_extractor.py:11-58）+ DBSCAN 聚类去重（clustering/dbscan.py:11-33 eps=0.10）
- 主动探索扩展（pipeline.py:1-120 EXPLORATION_SYSTEM_PROMPT 广度→深挖→模式）✗ 整条 offline 训练 pipeline 与主项目实时网关错配，只取 5-key schema + DBSCAN 防冗余

### Agent-Skills-for-Context-Engineering-main
- context-compression（skills/context-compression/SKILL.md）：三大策略（Anchored/Opaque/Regenerative）+ tokens-per-task 而非 tokens-per-request（:40-46）+ 6 维评分（:145-157）+ artifact trail 单独追踪（:68-82）
- probe-based 评估（:110-140）
- 模板规范（template/SKILL.md <500 行 + 第三人称 description :10-12）✓ compression 策略矩阵 + artifact trail 适用 SSE 长会话

### feichanggege__ecommerce-visual-copywriting-skill（黄金模板）
- frontmatter 三段式（SKILL.md:1-15 name/description 多行 YAML/description_zh+en/author/homepage/license + 触发词）
- 节序：语言规则→何时使用（触发词+反面清单）→核心原则（5 条可验证断言）→工作流（7 步带序号）→暂停句（:97-100 "以上是本次视觉策划案，请确认…"）→缺资料最多追问 3 问（:207-214）→失败模式（:216-225）→交付前检查清单 9 项（:226-240）
- 确定性领域判断表：转化驱动力 4 类（:77-88）+ 主图 5 张分配（KV1~KV5 :108-125）+ 详情页叙事流（:126-144）
- 五维独立自审（:184-206）0-100 各维 <80 只重写该维，"不用其他高分抵消"
- 合规规则库分层 references/compliance-rules.md（通用 + 蓝帽子 + 普通食品，:20-82）
- 发布验证 tools/verify-skill.py:10-70（REQUIRED_FILES + 关键字断言）+ test-prompts.json（3 prompt + expected_behavior）✓ **主项目技能库扩展的直接模板**

### dashi-ppt-skill
- frontmatter 极简（skills/dashi-ppt/SKILL.md:1-4 仅 name+description）
- 先列 30+ 条使用规则再列工作流（:48-119）+ 主题选择 hints 注释区（theme-choice-hints:start/end :79-101）
- 3+1 结构（:40-47）：3 模板锁文案 + 1 bespoke 独立设计
- 媒体纪律（:120-126）：只写 canPresetMedia 槽/禁绝对路径/file:///远程 URL/2 张以上并行 subagent
- 成果验收 3 态 + 修正 ≤2 轮（:146-176 通过/待修正/阻塞，>2 轮标阻塞不得交付）
- JSON 契约 schemaVersion:2（:178-247，canonical content 唯一业务事实源）+ 页面属性契约（:252-264）+ 三道校验链（:265-270）✓ 生成→验收类技能最佳参照

## 任务 E：MCP 类（4 项目）

### OWASP-MCP-Governance-and-Risk-Project
- Hard Gates 前置否决（mcp-governance-risk-framework-v1.0.md:1349-1398）：token passthrough 独立校验/超范围 token 拒 + 网络暴露（未认证内部数据/写执行/SSRF 可检索 URL/DNS rebinding）拒 + 生产（无 owner/无日志）拒
- 8 因子评分（:1404-1520 Data Sensitivity/Action Capability/Identity Scope/Exposure/Vendor Trust/Auditability/Reversibility/Blast Radius）+ critical-factor floor（任一=5 评级下限 High :1556-1562）
- 风险分带决策表（:1523-1555 8-15 Low/16-25 Medium/26-32 High/33-40 Critical CISO 批准）+ 评分工作表（:1556-1586 "每个分数必须有理由"）
- Pre-Approved Catalog(Paved Road)（:1399 CAT-01..06 让合规路径快于影子 IT）✓ MCP/新上游审批 schema 权威依据

### modelcontextprotocol__servers（官方参考）
- memory server 知识图谱 9 工具 CRUD 最小完整示例（src/memory/index.ts 745 行：create/delete entities+relations+observations/read_graph/search_nodes/open_nodes）
- search_nodes 含关系发现：一端命中保留（:637-652）
- 原子保存 temp file + rename(2)（:172-197）+ 失败清理临时文件 ✓ SQLite 经典安全写模式
- 同批去重双维度"已存在 + 批内前序"（:201-208）

### Windows-MCP
- SSRF/IP 白名单校验教科书（src/windows_mcp/infrastructure/security.py:12-78 validate_url 拒绝非 http(s)/内嵌凭证/private-loopback-linklocal-multicast-reserved）✓ 可原样移植代理池/URL 抓取
- stateless_http 模式（config.py:13-24 不发 Mcp-Session-Id 不维护连接态）✓ SSE 水平扩展对照
- 单工具聚合原则（tools/snapshot.py:25-73）
- 工具过滤（__main__.py:349-395）+ config CLI>配置>默认（:116-127）

### token-optimizer-mcp
- context_delta（src/tools/context-delta-tool.ts:1-70 对照上次快照 diff，首次整文件基线）
- 缓存引擎双栈 LRU + better-sqlite3（src/core/cache-engine.ts:34-50）+ 损坏 DB 自愈 isCorruptDatabaseError（:15-28 SQLITE_NOTADB/CORRUPT → 删重建降级内存 + degradedReason 显式报告）
- 语义缓存 0.85 阈值（:70-84）+ 自我测量仪表盘 ✓ 迁移 diff/自愈/语义缓存三模式

---

## 结论三条

1. **记忆层收敛**：mem0 增量提取 prompt + agentmemory 版本化/衰变/级联过时 + Tencent L0-L3 预算层 + ai-memory admission-before-LLM——纯 Python/SQLite 可直接落地，无付费上游依赖。
2. **skills 层**：立刻吸收 feichanggege + claude-skills 结构标准（frontmatter 三段式/暂停句/失败模式/检查清单/references 分层/verify-skill.py+test-prompts.json）+ book-to-skill 文档→技能流水线与成本预估。
3. **MCP 层**：核心是安全与可靠性——OWASP hard-gate 审批、Windows-MCP SSRF 校验、official memory 原子写与关系一端命中检索、token-optimizer 的 diff 与自愈。