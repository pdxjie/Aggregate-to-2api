# 参考项目深度侦察报告 · API 网关 / Agent 编排

> 侦察目标：`参考项目/` 目录下 11 个候选项目，提炼可迁移设计思想（非文件清单）。
> 主项目背景：Python 3.11 + FastAPI + SQLite(aiosqlite) + SSE 事件流的聚合 AI 图像网关（聚合 5 家图像上游 / provider 路由 / 号池 / 异步队列 / React 面板）。

---

# 任务 A · 网关 / 路由类

---

## 1. new-api

### 一句话定位
Go 实现的"新一代大模型网关与 AI 资产管理系统"（One API 的增强分支），聚合数十家上游、统一 OpenAI/Claude/Gemini 协议、含组织鉴权/额度/计费/任务平台（MJ/视频/图像）的完整商业化网关。

### 技术栈
Go + Gin + GORM + Redis（配额原子操作）+ SQLite/MySQL/PostgreSQL + React 前端。

### 最有价值设计点（附证据）

**D1-1. 渠道抽象为单一 `Adaptor` 接口，统一"请求转换→转发→响应转换"三段式**
`relay/channel/adapter.go:10-36` 定义 `Adaptor`：`GetRequestURL` → `SetupRequestHeader` → `ConvertOpenAIRequest` → `DoRequest` → `DoResponse`，同时 `TaskAdaptor` 接口（`adapter.go:41-88`）扩展出异步任务平台的完整契约：`ValidateRequestAndSetAction` / `EstimateBilling` / `AdjustBillingOnSubmit` / `AdjustBillingOnComplete` / `FetchTask` / `ParseTaskResult`。每个上游（openai/gemini/claude/zhipu… `relay/channel/` 下 40+ 目录）只实现这个接口即可接入。
> 迁移价值：主项目 `providers/` 已有 5 家图像上游，可把 `base.py` 收敛成"同步图/异步任务图"双接口（Estimate → AdjustOnSubmit → AdjustOnComplete 预扣三步），这对"图生图长任务 + 订阅额度"场景是现成范式。

**D1-2. `Ability` 多维索引表做"组×模型×渠道"笛卡尔可用性路由**
`model/ability.go:18-30` 定义 `Ability{Group, Model, ChannelId, Enabled, Priority, Weight, Tag}` 作为主键组合，`GetAllEnableAbilityWithChannels()` 一条 join 把渠道类型带出。路由层按 Group+Model 直接查 enabled 行集。
> 迁移价值：主项目 `adaptive_router.py` 当前是运行时打分；加一张 `ability` 预计算表可把"哪些号能用哪些模型"做成离线物化 + 索引查询，替代每次请求全量过滤号池。

**D1-3. 配额"先扣后退"三段式 + Redis Lua 原子操作**
`model/quota_reserve.go:20-66` 的 Lua 脚本：`Reserve` 先 `HINCRBY` 扣减、`ApplyDelta` 结算差值、`Refund` 幂等退还（`relay/common/billing.go:12-22` 注释明示 Refund 幂等安全）。Lua 脚本内含 `CacheSchema` 版本号与 `Id` 双重校验，防旧缓存误扣。
> 迁移价值：主项目有号池/额度场景可移植此模式——SQLite 无 Lua 原子性，可用"单行 UPDATE ... WHERE quota >= ? RETURNING"模拟 compare-and-swap；关键是"预扣-结算-退还"三段生命周期 + 幂等标记，比事后记账更防超卖。

**D1-4. Header 清洗/透传规则化（模板占位 + 正则 + 可选 passthrough 白名单）**
`relay/channel/api_request.go:51-300`：`SetupApiRequestHeader` 统一拼头；`applyHeaderOverridePlaceholders` 支持 `{apiKey}` 等模板占位；`IsHeaderPassthroughRuleKey` / `shouldSkipPassthroughHeader` 支持按渠道配置头透传/清洗。
> 迁移价值：主项目要防 `?api_key=` 泄漏（日志脱敏已做），但"按渠道配置头覆盖"与"UA 伪装/指纹清洗"可借鉴——图像上游对 TLS 指纹/UA 敏感，规则化的 header override 比硬编码白名单灵活。

**D1-5. `middleware/` 横向能力齐全：distributor / model-rate-limit / task-plugin / turnstile-check**
`middleware/` 目录 30+ 中间件：`distributor.go`（按比例分发）、`model-rate-limit.go`、`task_plugin.go`、`turnstile-check.go`、`request-id.go`、`body_cleanup.go`。
> 迁移价值：主项目 FastAPI 中间件层已有多项，但"模型级限流（每 model 独立令牌桶）"与"request-id 贯穿全链路（日志/SSE 事件关联）"值得对照补齐——后者能显著提升跨模块排查效率。

### 可直接迁移到主项目
- Ability 物化表（Group×Model×ChannelId 索引化可用性）
- TaskAdaptor 的"预扣-结算-退还"计费三段契约
- 渠道头透传/覆盖规则化配置
- 中间件横向能力清单（model-rate-limit、request-id、distributor）

### 主项目已有或更好的（不迁移）
- 主项目 MAB-EWMA 路由引擎（`adaptive_router.py`）按成功率/时延/负载打分，比 new-api 的 Priority+Weight 静态加权更先进
- SSE 事件流（主项目 `sse_events.py` subscribe/publish/replay + Last-Event-ID）比 new-api 的普通流式更完整
- Go 生态无法直接迁移代码，只能迁移模式

---

## 2. NewAPI-Gateway

### 一句话定位
聚合多个 new-api 上游的"透明网关"：单聚合 Token（`ag-xxx`）访问所有接入的 NewAPI 实例，做优先级分层 + 价值评分 + 健康调节的路由与透明代理，不做计费。

### 技术栈
Go + Gin + GORM + SQLite/MySQL/PostgreSQL + React。

### 最有价值设计点（附证据）

**D2-1. 优先级分层 + 同层"不放回的加权随机"重试计划**
`model/model_route.go:240-310`：`BuildRouteAttemptsByPriority` 先把候选按 Priority 降序分组为 `[][]RouteAttempt`，层内 `weightedShuffleAttempts`（`model_route.go:571-606`）按 Contribution 权重做"不放回加权抽签"，每层全部失败才降级到下一层。`controller/relay.go:70-100` 消费该计划：同层逐个尝试、不可重试错误立即返回。
> 迁移价值：主项目路由目前"选一个渠道就发，失败再重选"；改成"预生成重试计划（层级数组）"能消除运行中重复查库/重复筛选，且天然支持"同层加权多试几次、跨层降级"策略——与现有 retry_policy 可组合。

**D2-2. 透明代理三层防线：header 白名单清洗 + 代理特征头全删 + body 零改动**
`service/proxy.go:60-110`：仅转发 `Content-Type/Accept/Accept-Encoding/Accept-Language/User-Agent/anthropic-beta` 六个安全头；显式 `req.Header.Del("X-Forwarded-For"/"X-Forwarded-Host"/"X-Real-IP"/"Via"/"Forwarded")`；`rewriteRequestModel` 只改 body 中的 model 字段，其余零改动。上游感知不到网关存在。
> 迁移价值：主项目代理池已有住宅/免费双源；把"请求头白名单清洗 + X-Forwarded-* 全删"做进 `proxy_pool.py` 的请求构造层，能降低上游风控识别率，是目前未显式覆盖的点。

**D2-3. 价值评分 = 成本得分 × 预算得分**
`model/model_route.go:653-671`：`costScore = 1/(1+unitCostUSD)`（单价越低分越高）；`budgetScore = (balance+1)/(balance+recentUsage+1)`（余额相对近期消耗越充足分越高）。两者相乘作为路由权重。
> 迁移价值：主项目号池路由按成功率/时延打分，未纳入"余额/近期消耗"维度；把 `computeRouteValueScore` 移植成 Python，可让余额快耗尽的号自动降低权重，防把请求打到濒临欠费的账号。

**D2-4. 双 TTL 路由缓存：静态快照 60s + 运行指标 15s**
`model/model_route_cache.go:9-35`：`routingStaticSnapshotTTL=60s` 缓存 enabled 路由/归一化模型映射/提供商别名；`routingRuntimeMetricTTL=15s` 缓存运行时指标（256 key 上限防膨胀）。静态与动态分离刷新。
> 迁移价值：主项目每次请求实时算 MAB-EWMA 分数有 SQLite 压力；可拆"静态候选快照（分钟级）+ 动态指标（秒级）"双层缓存，减少重复查库。

**D2-5. 上游自动同步 + 响应有效性三态门（off/observe/enforce）**
`service/sync.go:20-60`：每 5 分钟从上游同步 pricing/tokens/balance，失败时按可达性分类打健康标记并自动重建路由表。`model/relay_reliability.go:11-100`：`RelayResponseValidityMode` 三态——`observe` 只记录无效响应、`enforce` 才拦截重试；配 `RoutingInvalidResponseSuppression*` 参数（阈值 3 / 窗口 10min / 冷却 15min）做无效响应抑制。
> 迁移价值：主项目有 `sync_deploy`/CI 同步，但"上游定价/余额主动拉取重建路由表"是新的；`off/observe/enforce` 三态灰度是主项目 `solver_guard.py` 熔断可以借鉴的渐进式上线方式。

### 可直接迁移到主项目
- 优先级分层 + 同层加权不放回抽签的重试计划生成
- 透明代理 header 清洗 + 代理特征头删除
- 成本×余额双因子价值评分
- 静态/动态双 TTL 路由缓存
- 响应有效性三态门（observe→enforce 渐进灰度）

### 主项目已有或更好的（不迁移）
- 主项目 MAB-EWMA + 熔断器（`adaptive_router.py`）动态性更强；NewAPI-Gateway 的评分是静态权重
- 主项目 SSE 事件流比其"SSE 流式透传"更完整（有 replay/Last-Event-ID）
- Go 代码不可直接迁移，只迁模式

---

## 3. litellm

### 一句话定位
Python 的事实标准 LLM 网关 SDK + Proxy：统一 100+ 提供商调用格式，附企业级路由/预算/缓存/多租户 Proxy 层。

### 技术栈
Python 3.10+ / pydantic v2 / httpx / FastAPI（proxy 层）/ Redis 缓存 / Postgres 持久化 / 可选 Rust bridge。

### 最有价值设计点（附证据）

**D3-1. 路由策略插件族：12+ 可插拔策略对象**
`litellm/router_strategy/` 下 `least_busy.py`（基于 in-flight 计数的最小忙碌）、`lowest_latency.py`、`lowest_cost.py`、`lowest_tpm_rpm.py`、`tag_based_routing.py`、`budget_limiter.py`、`adaptive_router/`（Thompson sampling bandit）等。`least_busy.py:22-30` 用 `_request_count_key(model_group, deployment_id)` 在 `DualCache`（内存+Redis）里计数。
> 迁移价值：主项目 `adaptive_router.py` 只有单一打分引擎；可按 litellm 方式抽 `RoutingStrategy` 基类 + 注册表，让"least_busy/最低成本/标签路由"变成可组合策略，而不是堆在引擎里。

**D3-2. Adaptive Router 用 Beta-Bernoulli Thompson sampling + 冷启动先验**
`litellm/router_strategy/adaptive_router/bandit.py:14-60`：每个 `(router, request_type, model)` 一个 `Beta(alpha,beta)` 后验 cell；冷启动先验 `initial_cell` 由模型质量分档 `BASE_TIER_WEIGHT` + 强项加成 `STRENGTH_BONUS` 构成，`COLD_START_MASS` 保证约 10 次真实观察即可明显移动先验；`thompson_sample()` 是纯函数无 I/O，走热路径。
> 迁移价值：主项目 MAB-EWMA 是频率派启发式；litellm 的 Beta-Bernoulli 贝叶斯后验 + 每 (请求类型×模型) 独立 cell + 带质量先验的冷启动，可直接移植到 Python——"图生图/文生图/聊天"请求类型分 cell 打分，天然处理冷启动号池。

**D3-3. `adaptive_router/signals.py` 会话级信号：turn 结构 + clean credit 分配**
`adaptive_router.py:12-30` 注释：`_session_states: (session_key, model) -> SessionState` 增量信号更新；`signals.py` 提供 `detect_response_signals` / `detect_user_feedback` / `advance_session_state`。只在"干净 turn"（无工具调用中断）才给选中的模型记 credit，避免把用户不满归因到错误模型。
> 迁移价值：主项目对图像任务很难有"会话"概念，但"失败归因"逻辑可借鉴——区分"上游明确报错"与"超时/网络中断"两类失败，后者不能全记到所选渠道头上。

**D3-4. 预算限制器、最低 TPM/RPM 策略与健康检查后台任务**
`router_strategy/budget_limiter.py` + `lowest_tpm_rpm.py`（预留吞吐量，选后仍满足 min_tpm/min_rpm 的部署）；router 内置后台健康检查循环，动态踢出/恢复部署。
> 迁移价值：主项目有熔断但无"吞吐预留"概念；图像上游并发配额敏感，`lowest_tpm_rpm` 思想可简化成"每个号保留当前并发占用数，选号时排除超配额号"。

**D3-5. Proxy 层与 SDK 分离 + `DualCache`（内存+Redis 双读）**
`litellm/proxy/`（FastAPI 企业网关）与 `litellm/` SDK 分离；`router.py:40-50` 引入 `DualCache/InMemoryCache/RedisCache/RedisClusterCache`。
> 迁移价值：主项目单机 SQLite 无 Redis，但"DualCache"抽象可落地为"内存 TTL 层 + SQLite 落底层"，与 D2-4 双 TTL 思路同源。

### 可直接迁移到主项目
- Beta-Bernoulli Thompson sampling bandit（含冷启动先验、按请求类型分 cell）
- 会话级信号 / 干净 turn credit 归因
- 可插拔路由策略基类与注册表
- DualCache 内存+持久化双读抽象
- 吞吐预留（min_tpm/min_rpm 简化为并发占用排除）

### 主项目已有或更好的（不迁移）
- 主项目 5 家图像上游规模小，litellm 的 100+ 适配器体系过重
- litellm 依赖 Redis/Postgres；主项目 SQLite 单机架构更简单
- 主项目 SSE 事件流 + 号池自动化是 litellm 没有的领域能力

---

## 4. 9router

### 一句话定位
面向 Claude Code/Codex/Cursor 等 CLI 工具的免费/便宜 AI 路由器：RTK token 压缩 + 订阅→廉价→免费三级自动降级 + 多账号轮询，Save 20-40% tokens。

### 技术栈
Node.js + Next.js（App Router）+ SQLite（better-sqlite）+ Python 外部进程（headroom 压缩代理）。

### 最有价值设计点（附证据）

**D4-1. 三级 fallback 链（订阅 → 廉价 → 免费），逐级耗尽再降级**
README 架构图明确：`Tier1 SUBSCRIPTION → quota exhausted → Tier2 CHEAP → budget limit → Tier3 FREE`，目标"零停机 + 最小成本"。路由查询 `src/mitm/handlers/base.js` 的 `fetchRouter` 统一入口。
> 迁移价值：主项目 providers 有免费/付费混合上游；"逐级降级 + 配额耗尽感知"的链式路由可直接映射到"付费上游（预算0 默认禁）→ 免费上游 → 兜底失败"，与红线兼容。

**D4-2. RTK token saver：旁挂独立压缩代理（headroom）**
`src/lib/headroom/process.js:50-80`：spawn 独立 headroom 二进制（`--code-aware` / `--disable-kompress` 开关），主进程只做进程管理（PID 探活、日志、端口）。压缩工具结果节省 20-40% tokens。
> 迁移价值：对图像网关无 tool_result 可压，但"旁挂独立压缩/预处理进程"的架构思想可迁移——如把 cf_solver/Turnstile 求解器、图片预压缩做成独立可插拔 sidecar，主进程只做生命周期管理。

**D4-3. MITM 协议翻译：OpenAI ↔ Claude 流式事件逐 chunk 转换 + 跨 chunk 状态机**
`src/mitm/handlers/kiro.js:40-140`：维护 `initKiroState`（toolCallInit/hasToolCalls/finishSent/usage/inThink/thinkBuf），`extractThinking` 用状态机处理跨 SSE chunk 的 `<thinking>` 标签片段；`base.js:146-214` 的 `pipeTransformedEventStream` 提供"逐 chunk transformFn + flush"的流式管道抽象。
> 迁移价值：主项目 SSE 事件流已很强，但"跨 chunk 的状态机翻译层"（把上游图像进度/轮询事件转成统一 OpenAI 风格 SSE）是可直接借鉴的模式——特别是流式字节/JSON chunk 的增量解析与跨 chunk 缓存。

**D4-4. 会话/账号池化管理（dashboard + connections + loginLimiter）**
`src/lib/auth/loginLimiter.js`、`src/lib/db/repos/connectionsRepo.js`：多账号登录态管理 + 登录限流。
> 迁移价值：主项目号池已有完整 `account_pool.py` + 邮箱池，此点不迁移；但"登录限流"可对照补齐到号池的账号切换逻辑。

**D4-5. Pxpipe 事件统计管道**
`src/lib/pxpipe/events.js:10-81`：`appendPxpipeEvent` / `readPxpipeEvents` / `getPxpipeStats` 按时间窗聚合。
> 迁移价值：主项目已有用量统计模块；事件管道 append/read/aggregate 三段式可简化现有实现。

### 可直接迁移到主项目
- 三级逐级降级 fallback 链（配额感知）
- 跨 SSE chunk 的状态机翻译层（上游事件 → 统一格式）
- 旁挂独立 sidecar 进程管理模式（cf_solver 已类似，可规范化）

### 主项目已有或更好的（不迁移）
- 号池/账号池（主项目 account_pool.py 远强于 9router 的 connectionsRepo）
- 代理池（主项目 proxy_pool.py 住宅+免费双源）
- 主项目是 Python/FastAPI，9router 是 Node/Next.js，工具链不兼容

---

## 5. nexus-llm-router

### 一句话定位
面向 AI 基础设施工程师的多 LLM 路由中间件：任务感知模型选择 + 成本优化 + 回退安全 + 可观测性，暴露 OpenAI 兼容 API（Observe→Decide→Act 状态机）。

### 技术栈
Python 3.11+ / FastAPI / structlog / Prometheus / OpenTelemetry / Presidio（PII）。

### 最有价值设计点（附证据）

**D5-1. 确定性复杂度/领域分类器（Logistic 回归 + 关键词特征），无 LLM 调用**
`src/classifier/complexity.py:11-55`：`LogisticComplexityClassifier` 对 6 个确定性特征（word_count/question_count/code_hits/medical_hits/legal_hits/instruction_hits）做逻辑回归打分；`DomainClassifier` 按关键词命中计数分类领域，且 `_TIEBREAK_PRIORITY`（`complexity.py:36-45`）故意让 MEDICAL/LEGAL 高优先域在平票时胜出（注释明示曾因 dict 顺序让 CODE 平票误路由）。
> 迁移价值：主项目图像任务可移植"零成本复杂度/类别判定"——如按 prompt 特征把任务分为"文生图/图生图/重绘/放大"，或按安全域过滤内容；`MEDICAL 优先平票`的教训说明 tiebreak 必须是显式优先级表而非隐式遍历顺序。

**D5-2. Observe→Decide→Act 显式生命周期 + 可插拔策略族**
`ARCHITECTURE.md`：`RECEIVED→CLASSIFIED→ROUTED→DISPATCHED→RESPONDED` + 独立 fallback 路径。`src/router/strategies.py:30-60`：`RoutingStrategy` ABC 每个策略 `choose(request, signals) -> RoutingDecision`（含 model/provider/strategy/rationale/fallback_chain）。决策结果显式带 `rationale`，写入审计日志。
> 迁移价值：主项目路由决策目前是黑盒打分；"每次决策带 rationale + 持久化"能让 `/v1/routing/records` 更好排查；策略插拔族（Rule/Classifier/CostOptimal/LatencyAware/AB）可作为演进蓝图。

**D5-3. 决策前硬预算门禁 + 估算成本预检**
`src/safety/budget.py:8-48`：`BudgetGuardrail.assert_can_spend(subject, estimated_cost_usd)` 在 dispatch **之前** 就拒绝"估算花费将超上限"的请求（`current_spend + estimated > cap + EPSILON` 即 raise）。每个 adapter 实现 `estimate_cost()`。
> 迁移价值：主项目红线"付费上游默认预算0"目前靠人工遵守；移植 `assert_can_spend` 到 dispatch 前可做成**硬门禁**——估算超预算的请求直接 402，这是最贴合红线的技术实现。

**D5-4. 每 provider 熔断 + 半开探活 + 回退计分板**
`src/safety/circuit_breaker.py:12-75`：`CircuitState{consecutive_failures, opened_at}`，`recovery_window_seconds` 到期自动半开，`is_half_open` 显式区分"半开探活窗口"；`src/router/fallback_scoreboard.py` 记录回退统计。
> 迁移价值：主项目熔断器在 `adaptive_router.py` 已实现；"半开探活窗口显式化"与"回退计分板"可对照补强——半开态决定是否放探针流量，是主项目可能缺失的细节。

**D5-5. PII 脱敏（正则 + Presidio 扩展点）+ 幂等键 + 虚拟密钥 + 多级限流**
`src/safety/` 目录：`pii.py`（正则红action + Presidio 扩展）、`idempotency.py`、`virtual_keys.py`（租户虚拟密钥）、`rate_limiter.py` + `tenant_rate_limiter.py`（API key 令牌桶 + 租户级）。
> 迁移价值：主项目已有 `mask_key()` 日志脱敏；"请求体 PII 红action 后置再转发"与"虚拟密钥（用户拿到的是映射 token，真实上游 key 不下发）"值得对照——后者与号池天然契合（用户不应直接持有真实账号密钥）。

### 可直接迁移到主项目
- 确定性 Logistic 复杂度/领域分类器（零 LLM 成本）
- dispatch 前硬预算门禁 `assert_can_spend`
- 决策 rationale 持久化 + 显式生命周期状态机
- 半开熔断探活窗口
- 虚拟密钥（号池映射 token）
- 显式 tiebreak 优先级表（非隐式遍历顺序）

### 主项目已有或更好的（不迁移）
- 主项目 MAB-EWMA 路由比 nexus 的规则策略更动态
- 主项目 SSE 事件流/replay 比 nexus 的普通 SSE 更强
- nexus 面向文本 LLM，图像适配器需重写（但其契约可参照）

---

## 6. free-router

### 一句话定位
本地 OpenAI 兼容网关：按模型排名 `free-best` 先选"该模型当前可免费提供"的 provider，首个"免费 + 可用 + 未冷却"者胜出，失败自动换下一个。

### 技术栈
Node.js 20+（纯 ESM .mjs，无框架依赖）+ 环境变量密钥 + 无数据库（内存状态）。

### 最有价值设计点（附证据）

**D6-1. "模型优先 + provider 候补"排序，而非 provider 优先**
README 明确：`free-best` 先**按模型**排名，再对每个模型尝试"所有当前提供该免费模型的 provider"；当前免费、有能力、未冷却的首个候选胜出。模型是主键，provider 是候补——与主项目"provider 优先再选模型"相反。
> 迁移价值：主项目图像场景"同一模型（如 SDXL）多家上游可跑"时，先按模型归一、再按号池状态筛候选，比先选号再验证模型少一次失败往返；`model/model_route.go` 已有 route 表，`free-best` 的"模型归一 → 候补链"可直接启发候选生成顺序。

**D6-2. 三级冷却机制（cooldown 时长按失败原因分级）**
`server.mjs:540-560`：`cooldownRemaining(candidate)` 从 `config.cooldownMs` 读不同失败原因的冷却时长；`server.mjs:655` 把错误消息分类成 `empty`（`/empty|reasoning only|no useful/i`），`server.mjs:872` 对"仅 reasoning 无内容/空流"单独判 `empty` 类型。
> 迁移价值：主项目可把"冷却时长"从单一常量升级为按失败分类（超时/限流/空响应/格式错）分级的 map；"reasoning only 或空流也算失败"对图像上游"返回空图/占位图"场景同样适用。

**D6-3. 冷却状态半持久化 + 明文密钥泄露保护 + 多路密钥查找**
`server.mjs` 冷却 map 常驻内存并在健康检查/路由时查询；`providers.mjs` 密钥查找顺序（进程 env → 项目 `.env` → `~/.hermes/.env`）首非空即胜，密钥绝不入 git。
> 迁移价值：主项目 `account_pool.py` 已有 SQLite 持久化号池；"密钥不入 config、多级 env 查找"与红线一致，可对照主项目 `IF_*` 前缀做审查。

**D6-4. SOCKS 代理解析 + NO_PROXY 白名单**
`proxy.mjs:3-60`：`parseSocksProxy` 解析 `ALL_PROXY/HTTPS_PROXY/HTTP_PROXY`，`noProxyList` 解析 `NO_PROXY` 逗号列表。
> 迁移价值：主项目 `proxy_pool.py` 已有住宅代理双源；"全量代理 env 解析 + NO_PROXY 白名单"可在测试环境（本地上游 mock）时做旁路，避免代理池把本机请求代理出去。

### 可直接迁移到主项目
- 模型优先 + provider 候补链的候选生成
- 按失败原因分级的冷却时长 map
- "仅 reasoning/空流视为失败"的空响应判定
- 代理 env 解析 + NO_PROXY 白名单（测试旁路）

### 主项目已有或更好的（不迁移）
- 主项目 SQLite 号池持久化远强于其内存状态
- 主项目代理池/SSE/多上游能力远超 free-router
- Node 单文件实现无法直接迁移代码

---

# 任务 B · Agent 编排类

---

## 7. hermes-agent-self-evolution-main（自演化闭环重点）

### 一句话定位
用 DSPy + GEPA（Genetic-Pareto 提示演化）对 Hermes Agent 的 skill/prompt/工具描述做"可测改进的自动演化"：变异 → 评估 → 约束门禁 → 选优，纯 API 调用无需 GPU。

### 技术栈
Python + DSPy + GEPA + pytest + LLM-as-judge。

### 最有价值设计点（附证据）

**D7-1. 演化闭环五阶段：读现状 → 生成 eval 集 → 变异 → 评估 → 约束门禁 → 选优**
README "How It Works" 图：`Read → Generate eval dataset → GEPA Optimizer ← execution traces → Candidate variants → Evaluate → Constraint gates (tests, size limits, benchmarks) → Best variant → PR`。关键：GEPA 读**执行轨迹**理解"为什么失败"而非只看"失败了"，再做定向变异。
> 迁移价值：主项目 `adaptive_router` 打分参数目前是手工调；把"路由决策记录（rationale + 失败原因）"作为执行轨迹喂给离线的参数演化器（变异 EWMA 权重/熔断阈值 → 回放历史请求评估 → 门禁选优），可形成"自演化路由参数"闭环，与 v12 的自反思方向一致。

**D7-2. 多维 fitness + LLM-as-judge + 长度惩罚**
`evolution/core/fitness.py:13-80`：`FitnessScore{correctness, procedure_following, conciseness, length_penalty, feedback}`，`composite = max(0, 0.5·correctness + 0.3·procedure_following + 0.2·conciseness - length_penalty)`；`LLMJudge.JudgeSignature` 用 DSPy 签名强制输出三维分数 + 可行动反馈。
> 迁移价值：把"路由质量评估"从单指标（成功率）升级为多维（正确性/协议遵循/成本/时延）+ 惩罚项，可用于主项目的路由决策质量回测与 provider 排序。

**D7-3. 硬约束门禁：候选必须全过才能入选**
`evolution/core/constraints.py:14-70`：`validate_all` 顺序跑 `_check_size`（体积上限）/`_check_growth`（相对基线不许膨胀）/`_check_non_empty`/`_check_skill_structure`（结构完整性），任一失败即拒；`run_test_suite` 要求 pytest 100% 通过。
> 迁移价值：主项目若做"路由参数自演化"，需同样的**约束门禁**——候选参数必须过回放测试 + 不越界（如探索率范围 0-0.2）才能上线，防止演化出危险参数。

**D7-4. 演化产物进 PR 而非直接提交（人为把关兜底）**
README Guardrails 第 5 条：`PR review — All changes go through human review, never direct commit`。
> 迁移价值：主项目红线框架一致——任何自演化/自动调参结果必须以"建议变更"形式呈现，经人审才生效；可直接纳入 workflow_status 的审批点。

### 可直接迁移到主项目
- 执行轨迹驱动的参数演化闭环（路由参数离线演化 + 回放评估）
- 多维 fitness（成功率/成本/时延/协议遵循 + 长度惩罚）
- 演化候选硬约束门禁（越界即拒 + 回放测试）
- 演化结果必须 PR/人工审核才生效

### 主项目已有或更好的（不迁移）
- GEPA/DSPy 依赖较新，主项目当前无该依赖；仅迁移模式
- 目标对象不同（agent skill vs 路由参数），实现需重写

---

## 8. mcp-agent-main（intent 分类/硬门禁重点）

### 一句话定位
基于 MCP 的可组合 agent 框架：实现 Anthropic 全部有效 agent 模式（workflow/router/orchestrator/parallel/swarm…），Temporal 持久化可选。

### 技术栈
Python 3.10+ / MCP / pydantic / FastMCP / 可选 Temporal / 可选 embedding（cohere/openai）。

### 最有价值设计点（附证据）

**D8-1. Intent 分类器双实现：embedding 相似度 or LLM 分类，共用同一 `IntentClassificationResult` 契约**
`src/mcp_agent/workflows/intent_classifier/intent_classifier_base.py:22-80`：`Intent{name, description, examples, metadata}` + `ExtractedEntity{name, value}` + `IntentClassificationResult{intent, p_score, extracted_entities}`；`intent_classifier_embedding.py` 用 `compute_similarity_scores/compute_confidence` 做概率分类。embedding 与 LLM 实现可互换。
> 迁移价值：主图像网关可用同一契约做"请求意图分类"——如"文生图/图生图/放大/风格转换"决定走哪个 provider 族或触发图生图互斥锁；embedding 版零 LLM 成本，可先落地。

**D8-2. 可插拔 Router（embedding 或 LLM）决策带分数与置信度**
`src/mcp_agent/workflows/router/router_*.py`：与 intent 同构的 router 抽象，选择输出带 `p_score`。
> 迁移价值：主项目路由决策可增加"置信度"输出，低置信度时自动走 fallback 或降级，而不是单点选优。

**D8-3. DeepOrchestrator 集中式 PolicyEngine：continue/replan/force-complete/emergency-stop 四态决策**
`deep_orchestrator/policy.py:16-60`：`PolicyEngine(max_consecutive_failures=3, min_verification_confidence=0.8, replan_on_empty_queue=True, budget_critical_threshold=0.9)`，`decide_action(queue_empty, verification_result, budget, iteration, max_iterations) -> PolicyAction`。参数全部显式配置化。
> 迁移价值：主项目异步队列缺"策略引擎"层——把"连续失败 N 次→replan/降级、预算用 90%→critical、队列空→收尾"集中成策略对象，比散落在 engine.py 的 if 判断可测试、可调参。

**D8-4. 计划五维校验器（MCP server/agent 名/任务名/依赖/任务有效性）**
`deep_orchestrator/plan_verifier.py:16-172`：`verify_plan` 分解为 `_verify_mcp_servers/_verify_agent_names/_verify_task_names/_verify_dependencies/_verify_task_validity`，每步独立产出校验结果。
> 迁移价值：主项目若做多 agent 编排，计划生成后必须先过五维校验再执行——"依赖闭环检测/任务重名/引用不存在 agent"这些都是主项目 dispatch 层目前未显式覆盖的。

**D8-5. SimpleBudget 三维度（token/cost/time）超额即返回超限标志**
`deep_orchestrator/budget.py:19-75`：`max_tokens/max_cost/max_time_minutes` 任一超限即 `(True, "Token budget exceeded: …")`；配合 `budget_critical_threshold=0.9` 提前进入 critical。
> 迁移价值：与 nexus 的 `assert_can_spend` 互补——nexus 是 dispatch 前硬拒，这里是执行中持续记账并在 90% 处预警；主项目可两者结合（事前预算门禁 + 事中预算监控）。

### 可直接迁移到主项目
- Intent 分类契约（embedding/LLM 可互换）+ 路由置信度输出
- 集中式 PolicyEngine（失败/预算/队列空 → 策略化决策）
- 计划五维校验器（依赖闭环/名称引用校验）
- token/cost/time 三维预算追踪 + 90% 临界预警

### 主项目已有或更好的（不迁移）
- 主项目 SSE 事件流比 mcp-agent 的 tracing 更适合图像任务实时进度
- mcp-agent 强依赖 MCP 生态，主项目暂无此需求
- 图像任务非长程 agent 研究，DeepOrchestrator 全套过重，只取策略/预算/校验三件

---

## 9. agent-orchestrator-main（任务分解重点）

### 一句话定位
Composio 出品的并行 AI 编码 agent 编排层：每个 issue 一个独立 git worktree 的 agent，自主修 CI/回评论/开 PR，人只做最终审查。

### 技术栈
TypeScript + Node 20+ + tmux + git worktree + GitHub/Linear（tracker 无关）。

### 最有价值设计点（附证据）

**D9-1. 递归任务分解：原子性判定 + 谱系上下文注入，直到叶子全原子**
`packages/core/src/decomposer.ts:94-115`：`DECOMPOSE_SYSTEM` 提示词明确 "When in doubt, choose atomic. Over-decomposition creates more overhead than under-decomposition."；`planTree` 递归直到叶子原子（`decomposer.ts:174-200`）；`formatLineage`/`formatSiblings`（`decomposer.ts:68-80`）把父链与并行兄弟注入子任务上下文。
> 迁移价值：主项目异步队列只有"任务 → 执行"两层；引入"递归分解 + 原子性判定 + 谱系注入"可让复杂生图任务（多步提示词增强→生图→后处理）自动拆为子任务并行，且每叶子可独立重试/审计。

**D9-2. 分解计划默认需人工批准再执行**
`decomposer.ts:52`：`Require human approval before executing decomposed plans (default: true)`。
> 迁移价值：与红线"不可逆操作需授权"一致——自动分解出的执行计划默认须人批；主项目做任务分解功能时应继承此默认值。

**D9-3. Convention over configuration：零路径配置 + hash 命名空间防碰撞**
`ARCHITECTURE.md`：`hash = sha256(configDir).slice(0,12)`，运行时目录 `~/.agent-orchestrator/{hash}-{projectId}/`，session 前缀自动生成；`generateSessionPrefix` 从 camelCase 提取大写字母。
> 迁移价值：主项目多 worker/多项目并发时，用"配置路径 hash + 前缀"做隔离目录/会话命名可防碰撞——如图生图互斥租约可按 hash 隔离命名空间。

**D9-4. 反馈闭环工具：CI 失败/评审评论 自动路由回 agent 重试**
`packages/core/src/feedback-tools.ts:67-152`：`generateFeedbackDedupeKey` 防同一反馈重复触发；`validateFeedbackToolInput` 校验工具输入；反馈报告序列化/解析有文件格式契约。
> 迁移价值：主项目 SSE 事件流可加"反馈回路"——上游失败事件去重后自动触发重试/降级，`dedupe key`（事件ID+任务ID）避免同一失败反复重试浪费号池。

**D9-5. 工作区钩子 + 生命周期管理器 + 活动日志 + 可观测性**
`packages/core/src/` 下 `agent-workspace-hooks.ts`、`lifecycle-manager.ts`、`activity-log.ts`、`observability.ts`。
> 迁移价值：主项目 worker 生命周期（分配→执行→回收）可对照补"活动日志 + 生命周期钩子"，让 worker 异常时能审计中途状态。

### 可直接迁移到主项目
- 递归任务分解（原子性判定 + lineage/sibling 上下文注入 + 人工批准默认）
- 反馈去重键（事件去重触发重试）
- hash 命名空间隔离（租约/会话命名防碰撞）
- 生命周期钩子 + 活动日志

### 主项目已有或更好的（不迁移）
- 主项目是异步队列（asyncio worker），无需 git worktree/tmux 这类编码 agent 基础设施
- TypeScript/tmux 依赖与主项目技术栈不同，只迁模式

---

## 10. OpenSwarm-main（多 agent 通信）

### 一句话定位
基于 Claude Code CLI 的自主 agent 编排器：Worker/Reviewer 结对流水线产出代码变更，Linear 接单、Discord 汇报、LanceDB 向量记忆。

### 技术栈
Node.js 22 + Claude Code CLI（可选 Codex）+ LanceDB + Xenova（本地 embedding）+ SQLite。

### 最有价值设计点（附证据）

**D10-1. Worker/Reviewer 结对 + 置信度阈值驱动的三种行动**
`src/agents/agentPair.ts:23-28`：`CONFIDENCE_THRESHOLDS = { PROCEED: 80, CAUTIOUS: 60, HALT: 60 }`——≥80 可批准、60-79 追加验证、<60 停止并让 reviewer 修订；`ReviewDecision = approve | revise | reject`；`pairPipeline.ts:348-410` 处理 worker 重试（`retry with clean slate`——用全新上下文重试，避免污染上下文）。
> 迁移价值：主项目"质量信心阈值三态（放行/谨慎/停止）"可直接用于生图任务验收——图生成置信度<60 时自动触发重绘或换上游，而非无条件接受输出。

**D10-2. AgentBus 结构化消息总线（type + sender/recipient + executionId）**
`src/agents/agentBus.ts:12-55`：`MessageType = step_started | step_completed | step_failed | context_update | file_changed | error | log | request | response`；`AgentMessage{id, timestamp, type, sender, recipient?, executionId, payload}`；`ContextUpdatePayload{key, value, operation: set|append|delete}`。
> 迁移价值：主项目 `sse_events.py` 的 publish/subscribe 是单向事件流；AgentBus 的"带 recipient 定向消息 + executionId 关联 + context_update 三态操作"可扩展出 worker↔worker 定向通信与共享上下文更新——多 agent 并行时避免广播风暴。

**D10-3. 认知记忆：Xenova 本地 embedding + importance 权重 + 压缩归档**
`src/memory/memoryCore.ts:15-48`：本地 embedding 流水线（无外部依赖），记录 `importance` 0-1（`memoryCore.ts:45`），检索按重要度加权；`memory/compaction.ts:10-30`：`ARCHIVE_THRESHOLD=0.7 / MIN_IMPORTANCE=0.1 / CONSOLIDATION_SIMILARITY=0.85`，相似度>0.85 的记忆合并，重要度<0.1 的归档，防向量库膨胀。
> 迁移价值：主项目有号池统计；"embedding + importance 权重 + 相似度合并归档"可做上游行为记忆——按号池成功/失败历史 embedding 聚类，识别"相似失败模式"预判冷却。

**D10-4. Pydantic 任务状态模型（多语言契约一致性）**
`src/task_state_model.py:8-60`：`TaskExecutionStatus = backlog|todo|ready|blocked|in_progress|in_review|decomposed|done|failed|halted`；`ExecutionState{status, blocked_reason, retry_count, confidence}` + `WorktreeState{lease_expires_at}`。
> 迁移价值：主项目任务表状态可对照补 `blocked_reason/retry_count/confidence` 三字段——`blocked_reason` 让卡住任务可诊断，`confidence` 与 D10-1 阈值联动。`lease_expires_at`（租约过期）与主项目 `edit_leases.db` 图生图互斥锁语义一致，可对照增强。

**D10-5. CostInfo + 累计成本追踪**
`agentPair.ts` 的 `CostInfo` + 状态栏显示 model/message/cumulative cost。
> 迁移价值：主项目已有多上游，给任务粒度记账（上游选择 → 成本回填）可作为成本优化闭环基础。

### 可直接迁移到主项目
- 置信度阈值三态（PROCEED/CAUTIOUS/HALT）+ 全新上下文重试
- AgentBus 定向消息 + executionId 关联 + context_update 三态
- 任务状态模型补充 `blocked_reason/retry_count/confidence`
- 认知记忆 importance 权重 + 相似度合并归档
- 租约过期字段（对照 edit_leases）

### 主项目已有或更好的（不迁移）
- 主项目 SSE 事件流（subscribe/publish/replay + Last-Event-ID）在单向事件上更强；AgentBus 补充的是定向/上下文维度
- 主项目无 Claude Code CLI 场景；Worker/Reviewer 结对编码逻辑不迁移，只迁置信度/消息模式
- 本地 embedding 依赖重，主项目可用 SQLite 简单相似度替代

---

## 11. agent-guardrails（安全门禁）

### 一句话定位
AI 编码 agent 的硬策略门禁：在工具层**硬阻断**破坏性命令（terraform destroy / DROP DATABASE / prisma shadow-db 误操作），无论 agent 或人类当时是否批准——"人在环"审批不足，需工具层兜底。

### 技术栈
Shell + jq（hook 脚本）+ Claude Code hooks.json/settings.json 集成 + 策略文档（policies/）。

### 最有价值设计点（附证据）

**D11-1. PreToolUse hook 在命令到达 shell 之前硬阻断，退出码即决策（0=放行 / 2=阻断）**
`claude-code/scripts/terraform-guard.sh:4-7`：`Exit 0 = allow, Exit 2 = block (message shown to user)`；脚本从 hook stdin 读 `{tool_input:{command}}`，`jq -r '.tool_input.command'` 提取命令做正则匹配。阻断时输出 BLOCKED + 具体模式 + 人工处理指引。
> 迁移价值：主项目 FastAPI 无 shell hook 层，但"工具调用前硬门禁 + 退出码三态（放行/阻断/缺依赖报错）"完全可移植为中间件/服务装饰器——如"生图任务提交前的内容安全门禁""付费上游调用前预算门禁"，阻断时返回结构化错误而不是靠事后补救。

**D11-2. 策略文档化：每个门禁有配套 `policies/*.md` 解释"为什么挡"**
`policies/` 下 `terraform.md/databases.md/kubernetes.md/cloud-providers.md/git.md`——阻断规则与人类可读的"为什么"分离。
> 迁移价值：主项目 `docs/SOP.md` 可对照；门禁规则（正则黑名单）应与解释文档分离，让规则可审查、可争议、可演进，而不是埋在代码里。

**D11-3. 黑名单正则分层：普通命令 vs `-auto-approve`/`--yes` 无确认变体**
`terraform-guard.sh:18-34`：`BLOCKED_PATTERNS` 含 `terraform destroy`、`terraform apply -auto-approve`、`tofu destroy`、`pulumi destroy --yes` 等，显式覆盖**无人类确认的破坏变体**——因为"apply"本身可接受，但"apply -auto-approve"不可。
> 迁移价值：主图像网关的"破坏性操作清单"应同样区分"可逆/需确认/不可逆"三档；尤其对号池/数据库的批量操作，`-auto-approve` 类无确认变体必须单独列挡。

**D11-4. 真实事故驱动：从 July 2026 Opus5 误 DROP 生产库事故提炼规则**
`README.md` 详细记录：Opus 5 在 ultracode 模式跑 `prisma migrate diff --shadow-database-url=$DATABASE_URL_UNPOOLED`，因 shadow db 指向 prod 而清空全表，开发者批准时没意识到 `diff` 会先重置 shadow db；`prisma-guard.sh` 因此对 `--shadow-database-url` 参数做专门匹配。
> 迁移价值：方法论可迁移——主项目的门禁清单应由"已发生事故"驱动（如号池误批量停用、租约死锁），而非凭空列规则；事故→规则→测试样例 三位一体。

**D11-5. 全局/项目双轨安装 + 干跑验证**
README 安装流程：global（合并进 `~/.claude/settings.json` 权限 deny + hooks.json）与 per-project 两轨；装完用 `echo '{"tool_input":...}' | guard.sh` 干跑验证。
> 迁移价值：主项目若做安全门禁中间件，应提供"全局默认 + 项目覆盖"双轨配置与干跑模式（dry-run 只报告不阻断），降低上线阻力。

### 可直接迁移到主项目
- 工具/接口调用前硬门禁中间件（退出码三态：放行/阻断/报错）
- 阻断规则与策略文档分离
- 可逆/需确认/不可逆三档破坏性操作清单
- 事故驱动规则（事故→规则→测试样例）
- dry-run 干跑模式 + 全局/项目双轨配置

### 主项目已有或更好的（不迁移）
- 主项目红线（预算 0、不自动 commit）已有声明；guardrails 提供的是"技术强制化"手段，两者互补
- Shell/jq 实现不可直接迁移，需落为 FastAPI 中间件
- Claude Code hooks 机制与主项目无关，只取门禁思想

---

# 横向提炼：对主项目最值得落地的 10 条迁移

| 优先级 | 思想 | 来源 | 落地建议 |
|---|---|---|---|
| P0 | **dispatch 前硬预算门禁** `assert_can_spend` | nexus D5-3 | 付费上游默认预算 0 从"人工遵守"变"代码强制"，估算超预算直接 402 |
| P0 | **模型优先 + provider 候补链 + 重试计划分层** | free-router D6-1 + NewAPI-Gateway D2-1 | 候选生成按"模型归一→号池候补→层级重试计划"，减重复查库 |
| P1 | **响应有效性三态门（off/observe/enforce）+ 无效响应抑制** | NewAPI-Gateway D2-5 | 图生图空图/占位图判定先 observe 后 enforce，防误杀 |
| P1 | **任务状态补 `blocked_reason/retry_count/confidence` + 置信度三态** | OpenSwarm D10-1/D10-4 | 卡住任务可诊断；低置信自动重绘/换上游 |
| P1 | **Beta-Bernoulli Thompson sampling 路由（按请求类型分 cell + 冷启动先验）** | litellm D3-2 | 替代/增强 MAB-EWMA，处理冷启动号池 |
| P2 | **静态/动态双 TTL 路由缓存** | NewAPI-Gateway D2-4 | 减少每次请求全量查 SQLite |
| P2 | **成本×余额价值评分** | NewAPI-Gateway D2-3 | 号池余额将耗尽自动降权 |
| P2 | **决策 rationale 持久化 + 执行轨迹驱动参数自演化 + 硬约束门禁** | nexus D5-2 + hermes D7-1/D7-3 | 路由参数离线演化，越界即拒，PR 审核生效 |
| P2 | **跨 SSE chunk 状态机翻译层** | 9router D4-3 | 上游事件 → 统一 OpenAI 风格 SSE |
| P3 | **透明代理 header 清洗 + 代理特征头删除** | NewAPI-Gateway D2-2 | 降上游风控识别率 |

# 明确不迁移的（主项目已有或更好）
- 号池/邮箱池：主项目 `account_pool.py`/`email_pool.py` 远超所有参考项目
- MAB-EWMA 路由引擎：比 new-api 静态权重、free-router 轮询更动态
- SSE 事件流（subscribe/publish/replay + Last-Event-ID）：比 nexus/9router 的普通 SSE 完整
- Go/Node 代码本体：全部只迁移模式，不迁代码
