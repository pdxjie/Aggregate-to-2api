# 参考项目深度侦察报告（桌面 / 浏览器 / agent 高价值杂项）

> 侦察时间：2026-09-11 ｜ 目标：为「React 19 前端 + Python FastAPI 后端 + Tauri 桌面」的 AI 图像/对话网关提炼可迁移设计思想
> 主项目现状：已有 web 管理面板 13 页 + 桌面版全栈化 sidecar（Electron/Tauri 混合）、agent DAG 编排、SSE 事件流、号池/代理池、cf_solver 求解器、FTS5（context-mode 同款路径）等。
> 结论分级：A = 高价值直接对标｜B = 值得借鉴某点｜C = 低价值仅参考

---

## 任务 I：桌面应用类

### 1. Aether-desktop-orchestrator ｜ 一句话定位

个人 AI 桌面助手（Hermes Agent 的 fork/包装站），主打"屏幕感知 + 上下文感知 + 多模型路由 + 插件生态"的常驻桌面 agent，提供下载落地页与 GitHub Pages 分发。

- **技术栈**：README 声明 = 桌面 GUI（glassmorphism UI）+ OpenAI/Claude/Ollama 模型路由 + Python/TypeScript 双语言插件 + YAML 配置（`README.md:76-102`）。注意：仓库仅 `README.md` + `index.html` 两个文件，无真实实现——是"文档级项目"（Aether 是 Hermes-CN-Desktop 的旧名/分支）。
- **有价值设计点**（均为 README 陈述，无代码证据，作模式参考而非实现证据）：
  1. **多模型智能路由 + fallback**：`model_preference` + `fallback_model` 双值配置，按任务复杂度决策，可中途换模型 → `README.md:83-84`
  2. **屏幕/上下文时间线**：`context_depth: 30`（秒）+ `screen_capture_interval: 1.5`，构建 temporal context map → `README.md:85-87`
  3. **云端/本地推理分流**：`local_inference: true` + `cloud_inference_threshold: 0.85`，本地能判就本地，否则上云 → `README.md:100-101`
  4. **多 agent 并行编排**（"conductor for digital orchestra"）：一 agent 盯邮箱、一 agent 审代码、一 agent 管日历 → `README.md:42-43`
- **可迁移**：
  - `context_depth`/`screen_capture_interval` 的**"上下文时间窗"配置哲学**：可映射到主项目任务 DAG 的「上下文保留窗口」与「SSE 心跳间隔」可配化（当前主项目 `sse_events.py` 是固定策略）。
  - `cloud_inference_threshold` 的**本地/云端分流阈值**：主项目桌面 sidecar 可加「轻量任务走本地规则引擎 / 重任务走 LLM」阈值门，节省上游 token。
- **不迁移**：项目无代码，仅概念。主项目 MAB-EWMA 路由引擎（`api/adaptive_router.py`）在"按成功率/时延/负载打分"上已远超其"按复杂度静态分配"。屏幕捕获/桌面自动化与主项目定位（AI 网关）无关。

---

### 2. Hermes-CN-Desktop ｜ 一句话定位

Hermes Agent 中文社区的 **Tauri v2 桌面外壳**：专注原生窗口、本地进程管理、文件对话框、托管 runtime 安装/更新/回滚、生产级 REST 代理层与 WS 中继；Agent runtime 与 Dashboard 在姊妹仓库。

- **技术栈**：Tauri v2 + Rust + React + TypeScript + pnpm workspace；`run.py` 提供开发入口；Rust 侧有 `capabilities/`（Tauri v2 权限声明）、`build.rs`、`e2e/`（Playwright？）→ `README.md:11, 88-99`
- **最有价值设计点**：
  1. **桌面外壳 = 传输桥而非业务层**："生产模式下通过 Rust command 代理 REST 与上传；Gateway 使用官方 `/api/ws`，打包态必要时通过 Rust WS 中继绕过 WebView 限制，并集中处理鉴权" → `README.md:88, 99`
  2. **托管 runtime 自更新链**："支持安装、更新、签名校验、健康检查和回滚本地 Hermes Agent 内核" → `README.md:96`
  3. **YOLO 高风险操作开关**："开启需二次确认，自动批准危险命令（对应后端 `HERMES_YOLO_MODE`），切换后自动重启内核生效" → `README.md:100`
  4. **运行时诊断页 + 日志页 + 健康状态面板**（Dashboard 之外的桌面侧运维面）→ `README.md:70-84`
  5. **Model 服务商配置页**：覆盖云端多家 + Ollama/vLLM/LM Studio/llama.cpp 本地部署 → `README.md:98`
- **可迁移（高价值）**：
  - **Rust command 代理 REST/上传 + WS 中继绕过 WebView 限制 + 集中鉴权**：这正是主项目桌面 sidecar 现状（web 面板直接 fetch API）的进阶方向——桌面版应把 API 调用从 WebView fetch 改为 Tauri command 代理，统一注入鉴权头、避免 `?api_key=` 出现在查询串、CORS 直接消解。**对标优先级 A。**
  - **托管 runtime 自更新/回滚链**：主项目桌面版有 sidecar（`desktop/backend/` 二进制 + start.bat），可加"检测 API 版本 → 下载/校验/回滚"闭环。
  - **YOLO 高风险操作二次确认开关**：主项目桌面版可对"批量删除任务 / 清空号池 / 修改鉴权 Key"加二次确认 gate（前端 dialog + 后端二次 token）。
  - **运行时诊断页**：主项目已有 Health 页，可借鉴"进程管理 + 日志 tail + 健康检查"三合一桌面运维页。
- **不迁移**：Agent runtime/Dashboard 本体（Hermes-CN-Core 在别的仓库，主项目已有 `api/main.py` 组装）。中文社区生态/飞书接入与主项目无关。

---

### 3. pi-agent-desktop ｜ 一句话定位

pi（个人极简 Codex 类编程 agent）的 **Electron 原生桌面客户端**，主打会话管理、SSE 流式、长期记忆 LTM、会话分叉/克隆、Extension UI Bridge。

- **技术栈**：Electron + Next.js（middleware.ts 为 Next 风格）+ React + TypeScript + Node `node:sqlite`（LTM 持久化）+ Vitest；`app/api/` 走 HTTP、`electron/` 主进程管理 AgentSession 生命周期 → `README.md:23, 96-120`
- **最有价值设计点**（有代码证据）：
  1. **项目级长期记忆 LTM**：`observations` + `memories` 双表 + FTS5 trigram 索引；`is_latest` 快照位 + `parent_id` 血缘；observe 去重窗口 60s；FTS 迁移用 `PRAGMA user_version` 门控、drop+rebuild 更换 tokenizer → `lib/ltm/sqlite-backend.ts:96-205`
  2. **FTS5 查询消毒**：`sanitizeFtsQuery` 对 CJK 用 trigram、剥离 FTS 运算符、引号包裹 token，短 token（<3 字符）单独处理（trigram 匹配不到）→ `lib/ltm/sqlite-backend.ts:24-43`
  3. **并发窗口重启限流**：3 次/60s 窗口内才允许重启，窗口外重置 → `electron/restart-policy.ts:1-22`
  4. **DNS-rebinding 防线**：所有 `/api/*`（含 GET 敏感读）都做 Origin 校验；CSP 与 Electron 共享同一 builder（`lib/csp.ts`），API 响应不注 CSP → `middleware.ts:19-63`
  5. **会话分叉/克隆**：API/UI 支持从任意节点 Branch，可 Clone 到普通目录或 Git Worktree（新分支）→ `README.md:37`
  6. **队列式 steer**：Enter 立即 steer、Alt+Enter 排队，可拖拽/键盘重排 follow-up → `README.md:30`
  7. **桌面侧进程树/崩溃恢复**：`electron/process-tree.ts`、`electron/crash-recovery.ts`、`electron/port-selection.ts`（端口自动避让）等可单测模块 → `electron/` 目录
- **可迁移（高价值）**：
  - **LTM 双表 + `is_latest` 快照位 + FTS5 trigram + user_version 迁移**：主项目已有 `edit_leases.db`、`account_pool.db` 等 SQLite，但**没有跨会话记忆库**。若加"agent 记忆"能力，此方案是经过验证的完整蓝图（含 CJK 检索坑：unicode61 tokenizer 无法匹配 CJK 子串，必须 trigram）。**对标优先级 A。**
  - **AgentMode 持久化**（自动写入 `.jsonl` 自定义节点，重载恢复历史模式）：主项目 DAG 编排的 run 元数据可加"模式快照"实现断点续跑。
  - **Origin 校验 + 共享 CSP builder**：主项目 API 已有 `IF_CORS_ORIGINS`，可加"WebView/桌面侧固定 Origin 白名单 + CSP nonce"防本地恶意页访问 `127.0.0.1:8100`。**对标优先级 B。**
  - **队列式 steer**：主项目 SSE 事件流可加"运行中消息排队/重排"UX，Enter vs Alt+Enter 双键语义。
- **不迁移**：Electron 本身（主项目桌面版已在 Tauri/Electron 混用）；pi 的会话文件 `.jsonl` 解析与主项目 DB 存储不同构。

---

### 4. UI-TARS-desktop ｜ 一句话定位

字节开源的 **GUI Agent 桌面应用**（UI-TARS 模型驱动），提供本地/远程电脑与浏览器操作；旗下 Agent TARS 是把 GUI Agent + Vision 塞进终端/浏览器/产品的通用多模态 agent 栈。

- **技术栈**：Tauri（apps/）+ Rust + pnpm monorepo（apps/ + packages/agent-infra/）+ `@ui-tars/sdk` + nut-js 键盘自动化；packages/agent-infra 内嵌了 `browser-use` 的浏览器 agent 实现 → `README.md:13-46, 78`
- **最有价值设计点**：
  1. **动作空间即契约（ACTION_SPACES 白名单）**：`click/left_double/right_single/drag/hotkey/type/scroll/wait/finished/call_user` 十个固定原语，模型输出被约束在这个枚举里 → `apps/ui-tars/src/main/agent/operator.ts:21-34`
  2. **Windows 粘贴输入兼容**：`type` 动作在 Windows 走"写入剪贴板 + Ctrl+V + 还原原剪贴板"绕过 IME 输入延迟 → `apps/ui-tars/src/main/agent/operator.ts:86-100`
  3. **屏幕截图物理/逻辑分辨率换算**：`physicalSize = logicalSize × scaleFactor`，按主显示器 `display_id` 取源 → `apps/ui-tars/src/main/agent/operator.ts:36-84`
  4. **本地/远程双 operator 抽象**：本地 NutJSElectronOperator 与远程 operators 并存，同一 ExecuteParams 契约 → `apps/ui-tars/src/main/remote/operators.ts`
  5. **Agent TARS 的流式工具展示 + Event Stream Viewer**：多工具流式输出、运行时设置含 tool 调用耗时统计、事件流查看器 → `README.md:74`
- **可迁移（B 级，方向性）**：
  - **动作空间白名单原语**：若主项目桌面版要加"GUI 自动化"能力，固定 `ACTION_SPACES` 枚举 + 前端渲染每个动作参数 schema，比自由文本 prompt 稳得多。主项目 DAG 节点类型也可类似收敛为枚举。
  - **Windows IME 粘贴兼容**：主项目 cf_solver/sidecar 若涉及键盘注入可直接抄这段（clipboard 写 + Ctrl+V + 还原）。
  - **截图分辨率换算**：主项目桌面版若做屏幕/截图类工具可用。
- **不迁移**：GUI Agent 本体（UI-TARS 模型 + vision + operator 栈）与主项目 AI 网关定位不符；浏览器 operator 实现与其内嵌的 browser-use 重复（见下）。

---

## 任务 J：浏览器 / 自动化类

### 5. browser-use ｜ 一句话定位

最主流的开源 **AI 浏览器 agent 库**（让 LLM 通过 action 原语操作真实浏览器），现主打三形态：托管云 / CLI skill / Python 库。

- **技术栈**：Python 3.11+ + Pydantic + Playwright 浏览器层 + httpx；`browser_use/` 分 agent / browser / controller / dom / llm / tools / skills / tokens / telemetry / observability → `README.md:97-130`、`browser_use/` 目录
- **最有价值设计点**（有代码证据）：
  1. **plan 状态机注入上下文**：模型输出 `plan_update`（整体替换）或 `current_plan_item`（推进下标）；状态机把 `done/current/pending/skipped` 渲染成 `[x]/[>]/[ ]/[-]` 文本注入 agent 上下文 → `browser_use/agent/service.py:1417-1462`
  2. **三级 nudge（replan / exploration / loop-detection）**：连续失败达到阈值注入"REPLAN SUGGESTED"提示；探索 N 步未建 plan 注入"PLANNING NUDGE"；循环检测器按重复/停滞升压注入 → `browser_use/agent/service.py:1464-1506`
  3. **循环检测器**：`_update_loop_detector_actions` + `_update_loop_detector_page_state` 追踪行为重复与页面停滞 → `browser_use/agent/service.py:1508-1527`
  4. **连续失败只计单动作步骤**：多动作步骤出错交给循环检测，不污染 `consecutive_failures` → `browser_use/agent/service.py:1233-1242`
  5. **连接错误重连等待**：CDP 断开时若正在重连则 `asyncio.wait_for` 等 `_reconnect_event`，成功则注入 `ActionResult(error='Connection lost and recovered')` 而非终止 → `browser_use/agent/service.py:1269-1285`
  6. **错误分级日志**：部分失败 WARNING、达到最大失败才 ERROR；对 `Could not parse response` 给模型输出格式提示 → `browser_use/agent/service.py:1294-1310`
  7. **Skill 动态注册为 action**：`_register_skills_as_actions` 把 skill 变 action handler，随 skill 列表动态变化 → `browser_use/agent/service.py:836-924`
- **可迁移（高价值，对标优先级 A）**：
  - **plan 状态机 + nudge 注入**：主项目已有 agent DAG 编排，但 DAG 是静态图；browser-use 的**运行时 plan 更新协议**（`plan_update` / `current_plan_item` 双通道）可直接借鉴到 DAG 节点执行器，让 agent 中途可改执行计划，且把计划状态渲染进上下文/SSE 前端。**对标优先级 A。**
  - **循环检测 + 连续失败计数策略**：主项目 `adaptive_router.py` 有熔断（上游维度），但缺**agent 行为级**的循环/停滞检测——这是 DAG 编排器缺的一环。**对标优先级 B。**
  - **连接错误重连等待语义**：主项目桌面 sidecar / cf_solver 断开时可用"标记 recovered 而非直接失败"的语义。
- **不迁移**：Playwright 浏览器 agent 本体（主项目不做网页操作 agent）；`multi_act` 等具体执行链。

---

### 6. crawl4ai ｜ 一句话定位

50k+ star 的 **LLM 友好的异步网页爬虫/抓取库**：把网页转成干净的 Markdown（带引用提示），供 RAG/agent/数据管线使用。

- **技术栈**：Python asyncio + Playwright 浏览器池 + SQLite 缓存 + Docker 服务器模式 + 云 API；模块含 `adaptive_crawler` / `async_dispatcher` / `content_filter_strategy` / `extraction_strategy` / `deep_crawling` / `domain_mapper` → `crawl4ai/` 目录
- **最有价值设计点**：
  1. **域级自适应限速器（RateLimiter）**：每个 domain 独立 `DomainState`；429/503 触发指数退避 ×随机抖动（0.75–1.25），成功逐步回降 delay → `crawl4ai/async_dispatcher.py:28-80`
  2. **崩溃恢复（resume_state + on_state_change）**：长爬取断点续爬 → `README.md:50`
  3. **Prefetch 模式 5-10x 更快 URL 发现** → `README.md:50`
  4. **缓存上下文 + cache_validator**：缓存命中免重爬 → `cache_context.py` / `cache_validator.py`
  5. **安全默认（Docker API）**：v0.9.0 起 auth 默认开、仅绑 loopback，body 视为不受信 → `README.md:46`
- **可迁移（B 级）**：
  - **域级指数退避限速器**：主项目有代理池轮换 + 5 上游，但上游侧限速是 MAB/熔断；若做"多上游并发调度"，crawl4ai 的 per-domain `RateLimiter`（带随机抖动防雷群）可直接抄成 per-provider 层。**对标优先级 B。**
  - **断点续爬 + 缓存校验**：主项目任务队列（`queue_store.py`）可加"resume_state 快照 + 结果缓存校验"，大任务中途崩溃可从断点续跑。
  - **安全默认哲学**：主项目 `IF_*` 生产模板已收紧 CORS/CSP，可对标"本地服务 auth 默认开、绑 loopback"。
- **不迁移**：爬虫本体/HTML 解析/Markdown 生成（主项目无网页爬取需求）。

---

### 7. stealth-browser-mcp ｜ 一句话定位

**反检测浏览器 MCP 服务**：用 nodriver（CDP 无头控制真实 Chrome 系）绕过 Cloudflare/anti-bot/login wall，给 MCP 兼容 agent 用。

- **技术栈**：Python + nodriver + CDP + FastMCP；模块含 `dom_handler` / `network_interceptor` / `cdp_element_cloner` / `proxy_forwarder` / `file_upload_security` / `http_security` / `persistent_storage` / `process_cleanup` → `README.md:9`、`src/` 目录
- **最有价值设计点**：
  1. **可信模型分层**：README 有 Trust Model 章节——浏览器操作按危险度分级授权 → `README.md:93`
  2. **安全边栏模块独立**：`file_upload_security.py` / `http_security.py` 把上传与 HTTP 安全单独成模块（可审计可测试）→ `src/` 目录
  3. **CDP 元素克隆器 + 响应阶段 hooks**：`cdp_element_cloner` / `response_stage_hooks` / `dynamic_hook_system` 组成"抓取 + 实时改响应"管线 → `src/` 目录
  4. **实例内存存储（线程安全 + 可序列化快照）**：`InMemoryStorage` 只存 `state/created_at/current_url/title/tabs` 等精简可序列化字段，多线程 RLock → `src/persistent_storage.py:4-64`
- **可迁移（B 级）**：
  - **代理转发 + 请求拦截的管线化**：主项目 `proxy_pool.py` 是轮换逻辑；若桌面版要内置"带代理的浏览器抓取"，可借鉴 `proxy_forwarder` + `network_interceptor` 的管线组合。
  - **风险分级 Trust Model**：与 Hermes YOLO 一致，主项目若做"浏览器/自动化 agent"应对高危操作分级授权。
- **不迁移**：nodriver 反检测本身（Cloudflare 侧由主项目 cf_solver 解决，不重复造）；MCP 服务形态与主项目不符。

---

### 8. agent-browser ｜ 一句话定位

**浏览器烟雾检查/验收 agent**：加载页面后从 console/网络/视觉多信号判断"是否真的成功"，面向 CI 与 agent 验收。

- **技术栈**：TypeScript monorepo（src/ + crates/agent_browser_core Rust 内核）+ Playwright provider + MCP server + CLI → `src/` 目录
- **最有价值设计点**：
  1. **多信号验收决策（evaluateActionDecision）**：`failOnConsoleErrors` + `failOnNetworkErrors` + `failOnBlank` + vision，各信号聚合为 `shouldFail/reasons/warnings` → `src/action/decisions.ts:16-58`
  2. **SSRF 防护（URL 安全检查）**：`BLOCKED_PROTOCOLS`（file/data/javascript/chrome/about）+ 敏感 query key 黑名单（token/key/secret/password）+ 内网 IP 识别（IPv4 段 127/10/172.16-31/192.168）+ localhost 需环境变量显式开启 → `src/browser/safety.ts:1-77`
  3. **视觉验收告警**：截图空白/模糊 → warnings 而非 reasons（不阻断但提示）→ `src/action/decisions.ts:39-45`
  4. **会话管理 + 用量计费（api/usage.ts）**：多会话隔离 + token 用量统计 → `src/sessions/manager.ts`、`src/api/usage.ts`
- **可迁移（B 级，但 SSRF 部分 A 级）**：
  - **SSRF URL 安全检查清单**：主项目若加"网页抓取 / 链接预览 / webhook 回调"类能力，`safety.ts` 的协议黑名单 + 内网段识别 + localhost 显式开启是现成蓝图。**对标优先级 A。**
  - **多信号验收决策**：主项目 E2E / 上游返回校验可借鉴"console + network + 视觉 + 空白检测"聚合打分替代单点断言。
- **不迁移**：Rust 浏览器内核；CI 定位与主项目不符。

---

## 任务 K：杂项疑似高价值

### 9. copilotkit ｜ 一句话定位

**agent-native 应用的 React SDK**：Generative UI、共享状态、human-in-the-loop 工作流，背后是 AG-UI 协议（Google/LangChain/AWS/微软等采纳）。

- **技术栈**：TypeScript monorepo（packages/react-core / react-ui / core / runtime-client-gql / channels-*）+ AG-UI 协议 v2 → `packages/` 目录
- **最有价值设计点**：
  1. **interrupt 状态机（框架中立）**：`ɵInterruptState` 把 pending/resume/cancel/expired 四态 + legacy 兼容归一化，toolCallId → toolResult 映射 → `packages/core/src/interrupt-state.ts:52-77`
  2. **Generative UI / 后端工具渲染 UI 组件**：agent 调后端工具返回 UI 组件直接在前端渲染 → `README.md:90-93`
  3. **共享状态层**：agent 与 UI 组件实时同读写一个 state → `README.md:94`
  4. **多通道适配**：同一 agent 后端 → web/React Native/Slack/Teams/Telegram/WhatsApp → `README.md:97-100`
- **可迁移（A 级，前端侧）**：
  - **interrupt/resume 协议**：主项目 SSE 事件流已很完善，但缺**"工具审批暂停/恢复"的 HITL 语义**。copilotkit 的 interrupt 状态机（pending→resume/cancel，带 toolResult 回填）可直接映射到主项目任务 DAG 的审批节点。**对标优先级 A。**
  - **Generative UI**：主项目 13 页面板 + agent 工具结果目前是"消息里贴 Markdown/JSON"；若 agent 工具要渲染图表/表单/确认框，copilotkit 的"工具返回组件 schema，前端注册渲染器"模式值得对标。**对标优先级 B。**
  - **共享状态**：任务详情页与 SSE 事件流可共享一个实时状态层。
- **不迁移**：copilotkit 全家桶（React SDK 完整引入 vs 主项目自研 hooks）；AG-UI 协议栈过重。

---

### 10. langflow ｜ 一句话定位

**可视化 agent/工作流构建平台**：拖拽式编辑器 + 内置 API/MCP 服务器，把每条 workflow 变工具，含 Langflow Desktop（Windows/macOS）。

- **技术栈**：Python + FastAPI + React + LangChain/LangGraph 组件库 + Docker + 桌面打包；monorepo 含前端编辑器与后端 graph runtime → `README.md:16-35`
- **最有价值设计点**：
  1. **可视化 graph 编辑器 + 源代码访问**：组件可用 Python 改 → `README.md:20-21`
  2. **交互式 playground 逐步调试**：step-by-step 控制 flow → `README.md:23`
  3. **一条 workflow 三种出口**：deploy as API / export JSON / deploy as MCP server → `README.md:24-25`
  4. **桌面版全依赖内置**：不装 Python 环境即用（对标主项目桌面 sidecar 的"免环境"思路）→ `README.md:30-32`
- **可迁移（B 级）**：
  - **workflow 三出口（API/JSON/MCP）**：主项目 DAG 若做导出，可导出 JSON（重放）+ OpenAI 兼容 endpoint（已具备）+ MCP 工具三种形态。
  - **playground 逐步调试**：主项目任务页可加"单步执行 DAG 节点"调试模式。
- **不迁移**：LangChain/LangGraph 依赖（主项目 DAG 自研，不强绑框架）；全套可视化编辑器工程量太大，主项目暂无低代码诉求。

---

### 11. LibreChat ｜ 一句话定位

**自托管多模型聊天前端**（ChatGPT 风格），聚合 Anthropic/OpenAI/Google/自定义端点，含 agents、Code Interpreter、web search、会话管理、Langfuse 可观测。

- **技术栈**：Node.js（api/ + client/）+ MongoDB/Redis（Docker 部署）+ React 前端；`CONTEXT.md` 是极罕见的领域词汇表文档 → `README.md:54-100`
- **最有价值设计点**：
  1. **领域语言词汇表（CONTEXT.md）**：把 Agent execution envelope / enrollment / fork / mailbox / subagent thread / trigger capability shield 等 30+ 关键术语一次性定义，跨团队零歧义 → `CONTEXT.md:1-29`（这是**文档实践**层面的最佳样本）
  2. **agent 运行信封（run envelope）**：请求在"进入 agent/provider/tool 初始化前"先被冻结成"最小可信标识符 + 校验过的协议负载"，运行时状态全部由标识符重水化 → `CONTEXT.md:4-5`
  3. **Agent 注册生命周期权威（enrollment）**：admission → provider-start fencing → terminalize → 等 trailing writes → provider drain 最后 ack；delete-all 持 owner fence → `CONTEXT.md:7`
  4. **子 agent 线程 + 唤醒（completion wakeup）**：子任务 detached 执行前预注册持久化 `continue` 触发器，进程崩溃不丢唤醒；父 agent 等子 terminal transcript 持久化后才收集 → `CONTEXT.md:15-16`
  5. **每 actor mailbox + expected-action receipt**：事件 actor 有独立投递排队 lane，处理完才放行后续投递 → `CONTEXT.md:26`
- **可迁移（A 级，语义层）**：
  - **run envelope / enrollment 生命周期纪律**：主项目 `worker/engine.py` 的任务生命周期可借鉴"admission 前冻结最小请求 + provider-start 前再检查 owner 删锁 + 终态后等全部 trailing write 再 ack"的严谨顺序，防止"任务取消时 provider 还在写入"竞态。**对标优先级 A。**
  - **子 agent 持久化唤醒**：主项目 DAG 的子节点若可 detached 运行，借鉴"预注册 durable continue 触发器"防崩溃丢唤醒。
  - **CONTEXT.md 词汇表实践**：主项目 `workflow_status.md` 可增加"领域术语表"节，低成本高收益。
- **不迁移**：Mongo/Redis 依赖（主项目 SQLite 定位更轻）；LibreChat 前端本体。

---

### 12. OpenManus ｜ 一句话定位

MetaGPT 团队的**极简多 agent 任务执行框架**（Manus 开源平替）：ReAct agent + planning flow + 工具集 + browser use 集成，3 小时原型起家。

- **技术栈**：Python + Pydantic + LLM 抽象 + `app/` 分 agent / flow / tool / schema；Browser Use CLI 3.0 作默认 MCP server → `README.md:86-90`、`app/` 目录
- **最有价值设计点**：
  1. **plan 状态机与显示符号**：`PlanStepStatus`（not_started/in_progress/completed/blocked）+ 渲染 `[✓]/[→]/[!]/[ ]` → `app/flow/planning.py:16-42`
  2. **PlanningFlow 双角色**：`planning_tool` + `executor_keys`，按 step type 选执行 agent → `app/flow/planning.py:45-75`
  3. **ReAct 抽象**：`think()` / `act()` 分离，`step()` 组合 → `app/agent/react.py:25-38`
  4. **工具全部 Pydantic schema 化**（bash/computer_use/crawl4ai/file_operators 等）→ `app/tool/` 目录
- **可迁移（B 级）**：
  - **plan 状态机符号渲染**：与 browser-use 的 `[x]/[>]` 一致——多项目都收敛到"状态标记注入上下文"，主项目 DAG 执行状态（pending/running/done/blocked/failed）应统一渲染成这类符号块进 SSE 与前端。**对标优先级 B。**
  - **PlanningFlow 双角色**：主项目已有 DAG 编排，可借鉴"plan 节点 + executor 节点分离、按 step type 路由执行者"。
- **不迁移**：ReAct agent 骨架（主项目 DAG 引擎已更成熟）；浏览器集成本体。

---

### 13. deep-researcher / autoresearch（研究 agent 家族）｜ 一句话定位

一组"自主研究/自主优化循环"项目：`autoresearch`（karpathy 思路的泛化，Claude Code/OpenCode/Codex skill）与 `multi-agent-deep-researcher`（Langflow JSON 工作流）——核心都是"机械指标 + 约束循环 + git 即记忆"。

- **技术栈**：autoresearch = 纯 SKILL.md 体系（Claude Code skill，`.claude/skills/`/`.agents/skills/` 多客户端同步副本）+ shell 脚本编排；deep-researcher 变体 = Langflow + LangGraph JSON 工作流（6688 行 JSON）→ `uditgoenka__autoresearch/plugins/autoresearch/skills/autoresearch/SKILL.md:1-80`
- **最有价值设计点**：
  1. **自动研究循环的 8 条铁律**：bounded by default（默认限制迭代数，unlimited 要显式 opt-in）、read before write、one change per iteration、mechanical verification only、automatic rollback（git revert）、TSV 结果日志、handoff via `handoff.json` → `SKILL.md:9-14` + README 表格
  2. **指令路由表驱动（Dispatch 表）**：按调用文本 `Metric:`/`Verify:`/裸 goal 分派到 Classic / Orchestrator / Wizard 三种模式，每次打 banner `[autoresearch] mode: ...` → `SKILL.md:15-27`
  3. **"Think in Code"范式**：让 agent 写脚本算结果而非把 50 个文件读进上下文（context-mode 同源哲学）→ autoresearch README:42-49
  4. **v2.1 架构瘦身**：monolithic SKILL.md（813 行 ~100K tokens）拆成 41 行路由文件 + 12 个自包含命令文件（94-120 行，每次调用仅 5-8K tokens），95% token 削减 → `autoresearch/README.md:72`
  5. **pi-autoresearch 的实验 JSONL 协议**：`.auto/log.jsonl` 结构 `{run, commit, metric, metrics, status: keep|discard|crash|checks_failed, description, segment, confidence}`，从 log 可完整重建状态机 → `davebcn87__pi-autoresearch/extensions/pi-autoresearch/jsonl.ts:1-50`
- **可迁移（A 级）**：
  - **实验循环协议**：主项目若做"agent 自主优化"（如自动调上游参数/自动生成 prompt 并比分数），`log.jsonl` + `handoff.json` + git-revert 闭环是完整蓝图。**对标优先级 A（若做自优化）**。
  - **SKILL.md 拆片瘦身**：主项目 `skills/` 若存在巨型 skill 文档，按"路由文件 + 子命令文件"拆分可显著降 token。
  - **Dispatch 路由表 + banner**：主项目 agent 命令入口可加"模式 banner + 参数条件路由表"。
- **不迁移**：Claude Code skill 形态（主项目是 FastAPI + React，非 Claude Code 插件）；Langflow JSON（与主项目 DAG 不同构）。

---

### 14. hermes-agent-main ｜ 一句话定位

Nous Research 的**自我改进 agent**（Hermes Agent 本体）：技能自动创建、FTS5 会话搜索 + LLM 摘要跨会话记忆、多平台 gateway、子 agent 并行、6 种终端后端。

- **技术栈**：Python（agent/ 模块化）+ 多平台 gateway（Telegram/Discord/Slack/WhatsApp/Signal）+ 终端后端抽象（local/Docker/SSH/Daytona/Singularity/Modal）+ FTS5 SQLite → `README.md:14-25`
- **最有价值设计点**：
  1. **MemoryManager 单集成点 + 外部 provider 限制一个**：内置 provider 永远第一，只允许一个外部 provider（防 tool schema 膨胀与后端冲突），一个 provider 失败不阻塞另一个 → `agent/memory_manager.py:1-77`
  2. **技能自举闭环**：复杂任务后自动创建 skill、skill 使用中自我改进、定期 nudge 持久化知识 → `README.md:21`
  3. **后台 sleep 唤醒（serverless persistence）**：Daytona/Modal 空闲休眠按需唤醒，近零成本 → `README.md:24`
  4. **批量轨迹生成 + trajectory 压缩**：为训练工具调用模型收集数据 → `README.md:25`
  5. **honcho 辩证用户建模 + agentskills.io 开放标准兼容** → `README.md:21`
- **可迁移（A 级）**：
  - **MemoryManager 单点 + 外部 provider 限制**：主项目若加记忆系统，直接用这个"内置 provider 优先 + 至多一个外部 provider + 失败隔离"架构，比"多后端自由注册"稳得多。**对标优先级 A。**
  - **后台休眠唤醒**：主项目桌面 sidecar / cf_solver 常驻进程可做"空闲休眠 + 请求唤醒"降资源占用。
  - **跨会话 FTS5 + LLM 摘要**：与 pi-agent 的 LTM 互补，主项目记忆库可"FTS5 检索 + LLM 摘要混合"召回。
- **不迁移**：多平台 gateway（主项目无 IM 需求）；6 终端后端（主项目桌面 sidecar 单形态）。

---

### 15. context-mode ｜ 一句话定位

**MCP 服务器**，解决上下文问题的另一半：沙箱工具把原始数据挡在上下文窗外（315KB→5.4KB），FTS5+BM25 索引会话事件供断点续上下文，推广"Think in Code"范式。

- **技术栈**：Bun + TypeScript（cli.bundle.mjs/server.bundle.mjs 打包）+ SQLite FTS5 + hooks（PreToolUse/PostToolUse/UserPromptSubmit/PreCompact/SessionStart/Stop）+ 11 个 MCP 工具 → `README.md:34-51, 86`
- **最有价值设计点**：
  1. **四面解决上下文问题**：Context Saving（沙箱工具）、Session Continuity（FTS5 索引 + BM25 检索）、Think in Code（ctx_execute 脚本化）、不强制精简 prose（明确引用 kimi-k2.5 基准证明激进 brevity 提示词降级推理）→ `README.md:38-51`
  2. **--continue 语义**：不 --continue 则上一会话数据立即删除（fresh session = clean slate）→ `README.md:41`
  3. **ctx_doctor 健康检查**：验证 runtimes/hooks/FTS5/plugin registration 全绿 → `README.md:84-86`
  4. **ctx_stats 量化节省**：per-tool breakdown、tokens consumed、savings ratio → `README.md:89`
- **可迁移（A 级，方向一致）**：
  - **会话续接 + FTS5 事件索引**：主项目 SSE 事件流已有 subscribe/publish/replay + Last-Event-ID，context-mode 的"事件索引进 FTS5 + BM25 检索"把"重放"升级为"语义检索"——这是主项目 SS E 体系缺少的补强。**对标优先级 A。**
  - **沙箱执行 + 只回结果**：主项目若给 agent 加"执行分析脚本"能力，应遵循"脚本结果 3.6KB vs 读 47 文件 700KB"的哲学。
  - **ctx_stats 可量化省 token**：主项目管理面板可加"SSE 事件节省统计"页。
  - **"不强制 prose 精简"的实证立场**：主项目 prompt 工程应避免过度 brevity 指令。
- **不迁移**：MCP 服务器形态本身（主项目不是 Claude Code 插件生态）。

---

### 16. claude-mem / claude-supermemory（跨会话记忆）｜ 一句话定位

两类 Claude Code 记忆插件：`claude-mem` = 持久记忆压缩系统（13.x 版本迭代成熟）；`claude-supermemory` = transcript 增量捕获 + 云端/本地记忆库。

- **技术栈**：claude-supermemory = Node.js hooks 体系（capture.js + lib/ 多工具函数 + statusline.js + .claude-plugin）；claude-mem = bun + 多平台安装脚本 + cowork/cursor-hooks 适配 → `claude-supermemory/plugin/hooks/` 目录
- **最有价值设计点**（supermemory 有代码证据）：
  1. **transcript 增量捕获（uuid 游标）**：`getLastCapturedUuid` 从 `~/.supermemory-claude/trackers/<sessionId>.txt` 读上次捕获 uuid，`getEntriesSinceLastCapture` 只取新条目 → `plugin/hooks/lib/transcript.js:22-60`
  2. **工具结果截断**：`MAX_TOOL_RESULT_LENGTH = 500`，`shouldIncludeTool` 白名单过滤工具结果入记忆 → `plugin/hooks/lib/transcript.js:10-15`
  3. **状态行实时显示捕获进度**：`statusline.js` + `writeState/readState`（capture count/status）→ `plugin/hooks/capture.js:62-69`
  4. **失败兜底**：无 apiKey / 无新内容 → `{continue: true}` 静默放行，不阻断主流程 → `plugin/hooks/capture.js:41-55`
  5. **信号式捕获（signal config）**：`getSignalConfig(...).enabled` 决定增量 vs 信号条目标记 → `plugin/hooks/capture.js:49-51`
- **可迁移（B 级）**：
  - **uuid 游标增量捕获**：主项目 SSE replay 若加"每会话记忆归档"，用"游标 + 仅新条目"模式可避免重复归档。**对标优先级 B。**
  - **工具结果截断 + 白名单过滤**：agent 记忆入库前的卫生策略，防止工具输出污染记忆库。
  - **状态行显示记忆进度**：主项目桌面版可加"记忆保存中/已保存"状态栏。
- **不迁移**：Claude Code hooks 形态；云端记忆服务（主项目本地 SQLite 定位）。

---

### 17. Graft ｜ 一句话定位

**开源代码图谱上下文层**：为 Claude Code/Cursor/Codex 建仓库知识图谱（图即 markdown 文件、git 即同步、无数据库），声称 4× 便宜 3× 快、正确性 +12pt，SWE-bench Verified 有数据。

- **技术栈**：TypeScript strict monorepo + LLM provider 抽象 + `graft init/build/ask/map/grep/viz`；图以"链接 markdown 文件"形式落盘（可提交 git，各队友本地 rebuild）→ `README.md:71-81`
- **最有价值设计点**：
  1. **图即文件、无数据库**：`.context/` / `graft/` 是文件夹 + 链接 markdown；`git` 即同步；CI 用 `check` 报告是否过期 → `src/engine.ts:1-11`
  2. **两级构建**：Tier-1 静态抽取（无 LLM）+ Tier-2 LLM 意义抽取（summary + crux），`--lsp` 可加编译器级边富化 → `src/engine.ts:41-53`
  3. **GraphRank 检索**：`ask/` 目录含 `graphrank.ts` + `fuse.ts` + `file-rank.ts`——图谱 + 模糊检索混合排序 → `src/ask/` 目录
  4. **文件提取缓存**：`graph/extract-cache.ts` 只重处理变更文件 → `src/graph/extract-cache.ts`
  5. **hosts 抽象**：`src/hosts/` 适配多 agent 客户端（Claude Code deep integration / MCP）→ `src/hosts/`
- **可迁移（B 级，主项目已有 graft 索引）**：
  - **图即文件 + git 即同步**：主项目已用 graft（`graft/` 已索引）。若要在产品内提供"代码知识库"能力（如管理面板显示依赖图），可借鉴"LLM 两级抽取 + 提取缓存 + check 过期"管线。
  - **提取缓存**：主项目 `graft build` 全量重建可改为"只处理变更文件"。
- **不迁移**：Graft 本体（主项目已内嵌使用，作为参考其 API 设计即可）。

---

### 18. ECC（everything-claude-code）｜ 一句话定位

**Claude Code 的完整 skill/agent/hook 体系与脚手架**：预置 agents/、skills/、hooks/、commands/、contexts/ 五层配置，多插件平台（.claude-plugin/.codex-plugin/.cursor/skills/.kiro/.opencode）同步分发。

- **技术栈**：Markdown 驱动（agents/*.md + skills/<name>/SKILL.md + JSON hooks）+ shell/Node hooks + agent.yaml + 多平台插件目录 → `RULES.md:18-35`
- **最有价值设计点**：
  1. **skill 规范模板化**：SKILL.md 必须带 YAML frontmatter（`name/description/origin`），`origin: ECC` vs `origin: community` 区分一/三方 → `RULES.md:24-28`
  2. **agent 规范**：agents/*.md + YAML frontmatter（`name/description/tools/model`），文件名小写连字符且必须匹配 agent name → `RULES.md:19-23`
  3. **hook 规范**：matcher-driven JSON 注册，matcher 要具体不要 catch-all；exit 1 仅在有意阻塞；错误信息要可操作 → `RULES.md:29-34`
  4. **多平台镜像目录**：同套 skill 同步到 `.agents/`/`.codex-plugin/`/`.cursor/`/`.kiro/`/`.opencode/` → ECC 根目录
- **可迁移（B 级）**：
  - **skill/agent/hook 的 frontmatter 规范**：主项目 `skills/`（superpowers-zh 20 个 skill）可对齐统一 `origin` 标记，便于区分自研/社区。
  - **hook matcher 具体化 + 可操作错误**：主项目已有 hook 体系（Windows 仅 Node hooks），可对齐"matcher 具体、错误信息可操作"。
- **不迁移**：ECC 本体（skill 目录规模与主项目不符）；其 agent.yaml 编排（主项目 DAG 更专门）。

---

### 19. OpenDocs ｜ 一句话定位

**文档自动生成器**：README/Markdown/Jupyter → 20 种格式（docx/pptx/pdf/LaTeX/Mermaid 图/知识图谱 HTML），5 家 LLM provider、25 主题，含 file watcher + auto-PR。

- **技术栈**：Python（src/opendocs/ 分 agents/generators/pipeline/publishers）+ LLM provider 抽象 + Mermaid/vis.js 渲染 → `src/opendocs/` 目录
- **最有价值设计点**：
  1. **管线抽象**：`pipeline.py` + `generators/`（多格式生成器）+ `publishers/`（多发布目标）+ `agents/`（docagent）→ `src/opendocs/` 目录
  2. **知识图谱导出**：实体提取 → 社区检测（label propagation 无外部依赖）→ god nodes + surprising connections + provenance 标签（EXTRACTED/INFERRED/AMBIGUOUS）→ `README.md:50-60`
  3. **File watcher + auto-PR**：`opendocs watch` 监控仓库变更自动重生成文档并开 PR → `README.md:65-66`
- **可迁移（B 级）**：
  - **provenance 标签（EXTRACTED/INFERRED/AMBIGUOUS）**：主项目若做知识图谱/记忆，给事实打"来源可信度"标签是低成本高价值。
  - **watch + auto-PR**：主项目文档/版本基线可做"变更自动触发文档重建"。
- **不迁移**：OpenDocs 本体（主项目无文档生成产品诉求）。

---

### 20. anything-analyzer ｜ 一句话定位

**全场景抓包 + AI 自动逆向分析工具**（Electron 桌面）：网页（内嵌浏览器 CDP）+ 桌面应用/终端/脚本（MITM 代理 8888）+ 手机/IoT（Wi-Fi 代理）统一汇入一个 Session，AI 生成协议逆向/安全审计/加密分析报告；本身也是 MCP client + MCP server。

- **技术栈**：Electron 35 + React 19 + TypeScript 5（与主项目前端同栈）+ MITM 代理 + CDP + MCP（stdio + StreamableHTTP）→ `README.md:7-9, 69-73`
- **最有价值设计点**：
  1. **全场景统一 Session**：CDP 网页抓包与 MITM 代理流量统一汇入同一 Session，AI 分析一并处理 → `README.md:59`
  2. **两阶段分析**：Phase 1 智能过滤噪声请求 → Phase 2 聚焦深度分析 → `README.md:63`
  3. **5 种分析模式**：自动识别/API 逆向/安全审计/性能分析/JS 加密逆向 → `README.md:64`
  4. **JS Hook 注入**：自动拦截 fetch/XHR/crypto.subtle/CryptoJS/SM2/3/4 加密调用 → `README.md:65`
  5. **流式输出 + 多轮追问**：报告实时流式 + 可继续追问 → `README.md:67`
  6. **双 MCP 角色**：既是 MCP Client 接入外部 server 扩展分析，也是内置 MCP Server 把抓包能力暴露给 Claude Desktop/Cursor → `README.md:69-73`
- **可迁移（B 级，但"双 MCP 角色"与"流式追问"值得对标）**：
  - **流式报告 + 多轮追问**：主项目 SSE 事件流已支持流式；若报告类输出可加"追问上下文续"。
  - **两阶段过滤再分析**：大量上游响应做分析时，先噪声过滤再聚焦，省 token。
  - **内置 MCP server 暴露能力**：主项目若想把"生成/路由/号池"暴露给外部 agent，可加一个内置 MCP 端点。
- **不迁移**：MITM 代理/JS Hook 逆向本体（与主项目 AI 网关定位无关）；Electron（主项目桌面已在 Tauri/Electron）。

---

## 汇总与主项目对标优先级

### A 级（高价值，主项目后续优先对标）

| 来源 | 可迁移设计 | 落到主项目哪里 |
|---|---|---|
| browser-use | plan 状态机（plan_update/current_plan_item）+ replan/exploration/loop 三级 nudge | `api/worker/engine.py` DAG 执行器 |
| browser-use | 循环检测 + 连续失败只计单动作步骤 | DAG 编排器行为级停滞检测 |
| LibreChat | run envelope + enrollment 生命周期纪律（删锁/start-fencing/trailing-write-drain） | `worker/engine.py` 任务生命周期 |
| pi-agent-desktop | LTM 双表 + is_latest 快照位 + FTS5 trigram + user_version 迁移 | 新增跨会话记忆库 |
| Hermes-CN-Desktop | Rust command 代理 REST/上传 + WS 中继 + 集中鉴权 | 桌面 sidecar 通信层 |
| Hermes-CN-Desktop | 托管 runtime 自更新/签名校验/回滚 | 桌面 sidecar 自更新 |
| copilotkit | interrupt/resume HITL 状态机 | SSE 事件流 + DAG 审批节点 |
| context-mode | FTS5+BM25 事件语义检索（SSE replay → search） | `api/sse_events.py` 补强 |
| hermes-agent | MemoryManager 单点 + 至多一个外部 provider + 失败隔离 | 记忆系统架构 |
| autoresearch/pi-autoresearch | log.jsonl + handoff.json + git-revert 自优化闭环 | 若做 agent 自优化 |
| agent-browser | SSRF URL 安全检查清单 | 若加网页抓取/webhook 能力 |
| UI-TARS-desktop | 动作空间白名单原语 | 若加 GUI/桌面自动化节点 |

### B 级（值得借鉴某点）

| 来源 | 设计点 |
|---|---|
| pi-agent-desktop | Origin 校验 + 共享 CSP builder；队列式 steer；AgentMode 持久化 |
| crawl4ai | 域级指数退避限速器；resume_state 断点续爬 |
| Hermes-CN-Desktop | YOLO 高风险二次确认；运行时诊断页 |
| OpenManus / browser-use | plan 状态符号渲染 `[x]/[>]/[ ]` 注入上下文 |
| copilotkit | Generative UI（工具返回组件 schema） |
| claude-supermemory | uuid 游标增量捕获 + 工具结果截断白名单 |
| OpenDocs | provenance 标签（EXTRACTED/INFERRED/AMBIGUOUS） |
| anything-analyzer | 两阶段过滤再分析；内置 MCP server 暴露能力 |
| ECC | skill frontmatter 规范（origin 标记） |
| Graft | 两级 LLM 抽取 + 提取缓存 + check 过期 |
| autoresearch | SKILL.md 拆片瘦身（95% token 削减） |

### C 级（不迁移 / 主项目已有更好）

| 来源 | 原因 |
|---|---|
| Aether-desktop-orchestrator | 无代码的 README 项目；主项目 MAB-EWMA 路由已超越其静态多模型配置 |
| UI-TARS-desktop | GUI Agent 本体与主项目定位不符 |
| crawl4ai 爬虫本体 | 主项目无网页爬取需求 |
| stealth-browser-mcp | 反检测由 cf_solver 解决，不重复造 |
| langflow | LangChain/LangGraph 绑定 + 编辑器工程量与主项目定位不符 |
| LibreChat 前端/Mongo 栈 | 主项目 SQLite 轻量定位 |
| claude-mem | Claude Code 插件形态，主项目不是插件生态 |
| OpenDocs 本体 | 无文档生成产品诉求 |
| anything-analyzer 逆向本体 | 与 AI 网关无关 |

---

## 侦察方法说明

- 每个项目按软约束读取 1-3 个关键文件：README 开头 + 1-2 个核心入口源码；共读 30+ 文件，20 个项目全覆盖。
- 代码证据精确到 `文件:行号`；README 陈述型结论已标注"READme 陈述、无代码证据"。
- 路径均为相对项目名的写法，便于主项目后续按名定位到 `参考项目/<项目名>/...`。
- 主项目内已有同类实现（MAB 路由、SSE 事件流、FTS5、graft 索引、号池/代理池）的地方已显式标注"不迁移/主项目已有"。
