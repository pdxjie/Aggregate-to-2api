# 参考项目深度侦察报告 — 视频 / 电商 / Captcha 三组

> 侦察对象：`参考项目/` 下 18 个候选项目。目的：提炼可迁移设计思想，供「Python+FastAPI+SQLite+SSE 的 AI 图像/视频生成网关」（下称**主项目**，已有 txt2img/img2img/txt2vid、worker 队列、cf_solver Turnstile 求解器、付费上游预算=0 红线）吸收。
> 证据统一格式：`项目内相对路径:行号`。未注明即「合理推断」，标注「已验证」为实际运行/读码确认。

---

## 任务 F — 视频类

---

### 1. MoneyPrinterTurbo

**一句话定位**：输入主题 → LLM 生成脚本/关键词 → 素材搜索 → TTS 配音 → 字幕 → BGM → 合成短视频的一站式 Python+FastAPI 工具。

**技术栈**：Python 3.11 / FastAPI / Pydantic v2 / Redis（可选）+ InMemory 双任务管理器 / Streamlit WebUI（另有 REST API）/ 多路素材源、多路 TTS 供应商。

**最有价值设计点**：

1. **参数矩阵 schema 化（Pydantic 全部显式化，含兼容层）**：`VideoParams` 把 60+ 生成参数（宽高比/转场/字幕/配音/BGM/字体/水印）全部声明为带默认值的类型化字段，`Field(ge/le/max_length)` 做边界校验，枚举用 `str,Enum` 子类（`VideoAspect` 还内聚 `to_resolution()` 返回分辨率）。见 `app/models/schema.py:91-161`。这是「参数矩阵 schema 化」的样板——所有上游供应商的参数差异被统一折叠进一个模型。
2. **「Pydantic 严格校验 + 配置宽容兜底」双轨**：请求体走 Literal 严格校验，但 WebUI 残留的旧配置值用 `_get_valid_ui_choice()` 宽容处理，避免升级后历史配置让服务起不来。见 `app/models/schema.py:56-71`、`schema.py:143-148`。
3. **任务状态机 + 失败阶段落库**：`state: int`（FAILED=-1/COMPLETE=1/PROCESSING=4）+ `failed_stage` + `error` 三件套，任务查询响应模型对「成功/失败中途」两种轨迹都给了 OpenAPI 示例。见 `app/models/const.py:25-27`、`app/models/schema.py:289-306`、`schema.py:370-408`。
4. **流水线异常兜底**：`start()` 用 try/except 把任何未预期异常转换成可查询的失败状态（`_mark_task_failed(task_id, "pipeline", ...)`），保证「任务一旦入队，状态永远可查、不会悬空」。见 `app/services/task.py:1585-1616`。
5. **任务调度失败回滚**：先写状态记录再入队，若线程启动/Redis 入队失败则 `delete_task` 回滚，避免 API 永久展示一个从未运行的任务。见 `app/controllers/v1/video.py:202-238`。

**可迁移到主项目**：
- **参数矩阵 schema 化**：主项目 txt2img/img2img/txt2vid 的上游参数现在是各 provider 各自为政；照 `schema.py:91-161` 的模式建立统一 `GenerationParams`（宽高比→分辨率、负向提示词、步数、seed、视频时长等），每个 provider 做「统一参数→上游参数」适配层，新增上游不改请求契约。
- **失败阶段落库**（`failed_stage`）：主项目任务表加 `failed_stage` 字段，DLQ/SSE replay 可直接定位失败在「文生图/放大/视频合成」哪一段。
- **流水线兜底 + 入队失败回滚**：主项目 worker 已有队列；补「入队前先建状态、失败即删」的原子语义与 `start()` 级兜底，杜绝幽灵任务。

**主项目已有或更好（不迁移）**：任务状态机（主项目更细）；SSE 事件流（主项目有 `sse_events.py` subscribe/publish/replay + Last-Event-ID，MoneyPrinter 只有简单轮询查询）；多任务管理器（主项目 worker 队列是核心）。

---

### 2. video-shotcraft

**一句话定位**：把 Claude Code/Codex 变成动效设计工作室的 Agent Skill——用「镜头配方卡（recipe cards）+ Remotion 模板」做电影感产品视频，**独立终检 Agent 在干净上下文按检查清单验收成片**。

**技术栈**：TypeScript / Remotion（React 视频渲染）/ vitest；skill 形态（SKILL.md + agents/ + references/）；workbench 为浏览器端 CapCut 式剪辑台。

**最有价值设计点**：

1. **独立终检 Agent（final-review）是核心资产**：明确「审查 Agent 必须在干净上下文、不参与制作、不给它制作理由/修改历史/预期结论」，检查清单按维度拆（P 产品目标/F 功能完整性/V 视觉方向/S 镜头卡与 Gallery 变体/B 分镜一致性/D 数据与素材安全/A 音频与节奏/Q 视觉技术质量），每条结论强制带编号+帧号或截图证据，最后按「必须修复/建议优化/无法验证」分级。见 `references/final-review.md:1-6`、`final-review.md:30-116`。
2. **缺失输入不猜测**：缺少审查输入时标记「无法验证」，不自行猜测制作基准；索引/卡片文档/demo 源码/参考样片互相冲突时明确报告冲突来源。见 `references/final-review.md:25-26`。
3. **CardDef 声明式配方卡 schema**：每张镜头卡是 `{id, name, category, durationInFrames, timing, width/height, component, schema[], durationProp}` 的声明式定义，`schema` 字段用联合类型声明可调属性（text/number/slider/color/select/boolean），`defaultsOf()` 从 schema 推导默认值，一套 schema 同时驱动「属性面板 UI、默认值、校验」。见 `workbench/src/cards/types.ts:4-71`、`types.ts:106-108`。
4. **30fps 编排 + 工程帧率换算**：卡片统一按 30fps 编排，工程 fps 不同时 `clipDefaultsFor` 换算时长并反向变速，播放速度不变——解决「卡片时长在不同工程帧率下漂移」的通用问题。见 `workbench/src/cards/types.ts:73-75`、`types.ts:97-104`。
5. **逐项检查 + 反「整体不错」**：报告格式明确「不要用没有证据的整体不错代替逐项检查，也不要为好看全标通过」。见 `references/final-review.md:115-116`。

**可迁移到主项目**：
- **独立终检 Agent 模式**：主项目在任务完成后可以让独立 Agent（干净上下文）按检查清单验收成片/成品图，逐项带证据（帧号/URL/SSE 事件序列）打分分级。这是「质量闭环」的最强样板，与主项目 worker+DLQ 天然互补——终检不过直接进重试/DLQ。
- **检查清单维度化 + 证据强制**：可套用其 P/F/V/S/B/D/A/Q 维度拆法到「文生图/图生视频」的终检清单（主题一致性/提示词忠实度/敏感内容/分辨率合规）。
- **声明式 schema 驱动 UI 与校验**（`types.ts:4-71`）：主项目前端若要做参数面板，可让后端 provider 声明 `PropField[]`，前端自动渲染表单——比手写每页表单可维护得多。

**主项目已有或更好（不迁移）**：Remotion 渲染管线本身（主项目是上游 API 生成，不需要本地渲染）；Workbench 浏览器剪辑台（超出网关范围）。

---

### 3. Pixelle-Video

**一句话定位**：输入主题 → 文案 → 配图规划 → 逐帧处理 → 视频合成的 AI 全自动短视频引擎（AIDC-AI 出品），把「素材/TTS/生图/视频合成」做成可插拔服务再被 pipeline 编排。

**技术栈**：Python / FastAPI（api/ 目录）/ Pydantic v2 / aiohttp / edge-tts / ComfyUI+RunningHub 工作流 / 直连媒体 API（Seedream/Seedance/Kling 等）/ Streamlit WebUI。

**最有价值设计点**：

1. **BasePipeline 抽象 + 服务挂载**：所有 pipeline 继承 `BasePipeline`（只实现 `__call__(text, progress_callback)`），通过 `self.core` 访问全部核心服务（llm/tts/media/video），pipeline 之间互相独立。见 `pixelle_video/pipelines/base.py:28-93`。这是「一个入口多个流水线变体」的插件化样板。
2. **结构化进度事件 ProgressEvent**：进度不是裸 float，而是 `{event_type, progress, frame_current, frame_total, step, action, extra_info}` 的 dataclass，`__post_init__` 强制校验 0-1，UI 层据此翻译展示。见 `pixelle_video/models/progress.py:23-63`。
3. **Storyboard 数据模型承载全生命周期**：`StoryboardFrame{index, narration, image_prompt, audio_path, media_type, image_path, composed_image_path, video_segment_path, duration}` 把「每一帧的中间产物路径 + 元数据」结构化，`Storyboard.progress` 属性直接用「已完成的帧/总帧数」算进度（`progress.py` 之外，见 `pixelle_video/models/storyboard.py:59-133`）。
4. **PixelleVideoCore 服务门面**：`service.py:43-96` 一个 core 聚合 llm/tts/media/video 四类服务 + 懒加载 `_get_or_create_comfykit`（配置哈希缓存），`async __aenter__/__aexit__` 管理生命周期。
5. **API 层与任务层分离**：`api/tasks/models.py:23-29` 的 `TaskStatus(PENDING/RUNNING/COMPLETED/FAILED/CANCELLED)` + `Task{task_id, task_type, status, progress, result, error, created_at/started_at/completed_at, request_params}` 是干净的异步任务契约；`api/tasks/manager.py` 负责 create/execute/cancel/cleanup 生命周期。

**可迁移到主项目**：
- **结构化进度事件**：主项目 SSE 现在按事件类型发进度；可升级为 `ProgressEvent` 结构化字段（step/action/子项进度），前端进度条与日志面板共用一份数据。
- **Pipeline 抽象**：主项目 txt2img/txt2vid 是单 worker；若要做「图生视频」「数字人」等新链路，照 `pipelines/base.py` 各自实现 pipeline 而共享 core 服务，避免主 worker 无限膨胀。
- **中间产物路径结构化**：`StoryboardFrame` 的产物路径建模适合主项目「生成→放大→合成」多阶段任务的 DB 表设计。

**主项目已有或更好（不迁移）**：任务模型（主项目更完整，含 SSE/DLQ/租约）；进度事件（主项目已有 subscribe/publish/replay）。

---

### 4. AI-Youtube-Shorts-Generator

**一句话定位**：长视频 URL → Whisper 转录 → LLM 高亮片段排名 → 智能裁剪 → 竖屏 Shorts 的开源 Opus Clip 平替。

**技术栈**：Python / yt-dlp / faster-whisper / OpenAI 兼容 LLM / ffmpeg+opencv；`llm_fn` 可插拔，CLI 与 Python 库双形态。

**最有价值设计点**：

1. **LLM 可插拔（`llm_fn: Callable[[str], str]`）**：同一套高亮提示词既能驱动 MuAPI 也能驱动本地 LLM，API/local 两种模式共享下游输出形状。见 `shorts_generator/highlights.py:20`、`highlights.py:272-304`。
2. **失效 JSON 自动重试 + 再提示**：LLM 输出解析失败时重试，且重试 prompt 追加「Return ONLY valid JSON… No markdown fences」的纠正指令——把「模型输出不听话」变成可控循环。见 `shorts_generator/highlights.py:200-248`。
3. **宽松解析 + 白名单清洗**：`_parse_json_loose` 剥 markdown 围栏；`_sanitize_highlights` 对每字段做类型强制（`_coerce_float/int`）、数值钳制（score 0-100）、字段裁剪——LLM 输出被当成不可信外部数据清洗。见 `shorts_generator/highlights.py:98-161`。
4. **要求 2× 候选再按分去重**：向 LLM 要「目标数量的 2 倍」高亮，然后用「重叠 >50% 则丢弃低分者」的贪心去重（按分数降序保留），既保数量又有选择空间。见 `shorts_generator/highlights.py:208-212`、`highlights.py:251-269`。
5. **长视频分块 + 偏移回填**：>30 分钟自动切 20 分钟块（1200s 窗口、重叠），块内时间戳 + chunk offset 回填成全局时间戳，最后再统一去重。见 `shorts_generator/highlights.py:64-65`、`highlights.py:287-300`。

**可迁移到主项目**：
- **LLM 结构化输出三件套（宽松解析→白名单清洗→失败重试+纠正提示）**：主项目若有「LLM 生成提示词/文案」环节，这套是生产级范本，可消掉大量脏数据。
- **2× 候选 + 贪心去重**：适用于主项目的「候选提示词/候选素材」场景。
- **llm_fn 可插拔**：主项目 provider 已多源，LLM 服务同样做成 callable 注入便于测试（符合 Mock 预算=0 红线）。

**主项目已有或更好（不迁移）**：剪裁/字幕烧录（不在网关范围内）。

---

### 5. CosyVoice

**一句话定位**：阿里 FunAudioLLM 的开源 TTS 大模型系（v1/v2/v3），支持零样本音色克隆、跨语言、指令控制（情绪/方言/语速/音量）、150ms 低延迟双向流式。

**技术栈**：Python / PyTorch / flow matching + hifigan vocoder / LLM 自回归 tokenizer / vLLM+Triton TRT-LLM 推理加速 / FastAPI 服务端 / Gradio demo。

**最有价值设计点**：

1. **一个 AutoModel 门面多推理模式**：`AutoModel()` 工厂按模型目录选类（CosyVoice/CosyVoice2/CosyVoice3），统一暴露 `inference_sft / inference_zero_shot / inference_cross_lingual / inference_instruct / inference_vc` 五类能力，`stream=True` 切流式。见 `cosyvoice/cli/cosyvoice.py:228`、`cli/cosyvoice.py:80-133`。
2. **语音通过「特殊 token」做细粒度控制**：文本中直接嵌入 `[laughter] [breath] <strong>勇气</strong> <laugh></laugh>` 等 token 控制停顿/笑声/强调/情绪；`add_zero_shot_spk(prompt_text, prompt_wav, id)` 把一次克隆音色注册成可复用 spk_id（`save_spkinfo()` 持久化），避免每次推理都传参考音频。见 `example.py:28-37`、`cosyvoice/tokenizer/tokenizer.py:248-271`。
3. **Instruct 模式（instruct2）**：`inference_instruct2(text, instruct_text, prompt_wav)` 用「用四川话说这句话」这种自然语言指令控制方言/情绪，比参数旋钮更灵活。见 `example.py:62-67`、`cli/cosyvoice.py:177-183`。
4. **RAS 采样 / KV cache / SDPA 等推理稳定性与加速**：官方 roadmap 显式列了 Repetition Aware Sampling 防复读、流式 kv cache 降 RTF。见 `README.md:37-45`。

**可迁移到主项目**：
- **TTS 供应商抽象模式**：`AutoModel` 工厂 + 统一推理方法签名，可映射为主项目「多 TTS 供应商（edge-tts/openai-tts/CosyVoice 本地）」的适配层，zero_shot spk 注册/持久化可做成「音色池」资源。
- **细粒度控制 token 设计**：若主项目做「视频解说/带情绪配音」，可借鉴在文本里嵌控制 token 的协议。
- **instruct 模式**：把「方言/语气」从参数旋钮升级为自然语言指令。

**主项目已有或更好（不迁移）**：核心 TTS 模型权重（这是模型仓库不是服务，主项目只需接它的服务端/API）；流式（主项目是异步任务不是实时通话）。

---

### 6. VideoCaptioner

**一句话定位**：基于 LLM 的字幕全流程工具——语音转录（多 ASR 引擎）、字幕优化/翻译（多翻译服务）、配音、合成一站式，CLI+GUI+Claude Code Skill 三形态。

**技术栈**：Python / faster-whisper / whisper-api / 必剪/剪映免费识别 / 必应/谷歌免费翻译 + LLM 翻译 / FFmpeg / Typer CLI / PySide GUI。

**最有价值设计点**：

1. **多引擎 + 免费兜底**：ASR 引擎五选一（faster-whisper / whisper-api / 必剪免费 / 剪映免费 / whisper-cpp），翻译服务四选一（llm / bing 免费 / google 免费 / deeplx）——「付费优先但免费可兜底」的供应商矩阵思路。见 `README.md:9-34`。
2. **配置优先级链 + 密钥掩码**：`命令行参数 > 环境变量(VIDEOCAPTIONER_*) > 配置文件 > 默认值`；配置类统一 `_mask_key()`（`key[:4]...key[-4:]`），`print_config()` 打印任务配置时永不泄露完整密钥。见 `videocaptioner/core/entities.py:577-623`、`entities.py:649-655`。
3. **枚举集中管理 + 每枚举带能力映射**：几十个枚举（字幕布局/渲染模式/视频质量/语言）集中在 `entities.py`，`VideoQualityEnum.get_crf()/get_preset()` 把「清晰度→ffmpeg CRF/preset」映射内聚；`get_asr_language_capability(model)` 返回每 ASR 引擎的语言能力表。见 `videocaptioner/core/entities.py:26-179`、`entities.py:469-521`。
4. **结构化任务配置 dataclass（TranscribeConfig/SubtitleConfig）**：把某类任务的几十个参数收敛进一个 dataclass，并自带 `print_config()` 输出可读任务摘要。见 `videocaptioner/core/entities.py:551-660`。
5. **业务常量集中**：`core/constant.py` 集中定义 UI 提示时长等常量。

**可迁移到主项目**：
- **配置优先级链（CLI 参数 > 环境变量 > 配置 > 默认值）**：主项目配置目前走 pydantic-settings，可加 CLI 覆盖层。
- **密钥掩码统一 `_mask_key`**：主项目已有 `mask_key()`，但 VideoCaptioner 把掩码做成配置类方法并让 `print_config` 全走它，可借鉴「任何打印配置的地方都过掩码」的纪律。
- **多引擎供应商矩阵（付费优先+免费兜底）**：与主项目多上游思路一致，可补充「免费优先策略」维度。

**主项目已有或更好（不迁移）**：枚举/配置集中在 config 包（主项目已做）；字幕功能本身不在主项目范围。

---

## 任务 G — 电商/商务类

---

### 7. EcomAgent

**一句话定位**：把电商运营 Agent 当「运行时系统」做的 FastAPI+React 底座——任务编排、Tool 安全控制（RBAC+业务规则+模式门+HITL）、执行可靠性（错误分类重试/幂等键/结果校验）、行为回归 harness。

**技术栈**：Python 3.11 / FastAPI（REST+WebSocket 双通道）/ React 18+TS / httpx / MCP（子进程 stdio）/ pytest（366 passed）/ SQLite 记忆。

**最有价值设计点**：

1. **错误四分类驱动重试**：`CLIENT_ERROR/BUSINESS_ERROR 不重试，TRANSIENT_ERROR(429/503/超时) 指数退避，FATAL 默认不重试`；HTTP 状态码与平台业务码双码表映射（`_HTTP_STATUS_CATEGORY` / `_PLATFORM_CODE_CATEGORY`），`classify_exception` 用鸭子类型识别带 `code` 属性的上游异常避免反向依赖。见 `execution/error_classification.py:13-80`。
2. **写操作幂等键**：`idem_{session_id}_t{turn}_{tool_name}_{params_hash}`（sha256 参数规范化哈希取 8 位），`should_attach` 按工具名前缀（query_/get_/list_/search_）自动跳过只读工具，重试复用同一把键由上游去重。见 `execution/policy/idempotency.py:13-62`。
3. **PreToolUse 管线（RBAC→业务规则→模式门→HITL）**：四角色工具级 ACL + 业务规则（如「调价不得低于成本价」）+ `READONLY/ASK/EXECUTE` 模式门；ASK 写操作发 `permission_request` 事件并**挂起整个 turn**，批准后从挂起点恢复，拒绝原因回传模型重新规划。见 `permission/pre_tool_use.py:21-82`。
4. **一份事件流三个消费方**：八类事件（`text_delta/tool_start/tool_result/permission_request/typed_error/status/complete/abort`）同时驱动前端 UI、评测 Harness 断言、执行轨迹回放，可观测字段（trace_id/attempt/duration_ms/idempotent_replay）是**在事件上增补**而非新设计。见 `README.md`「AgentEvent」段。
5. **Mock Commerce API 两阶段确定性故障注入**：故障在「副作用落库之后」才让客户端超时——精确复现「服务端已生效、客户端超时」的分布式最危险场景，端到端验证重试命中幂等键不产生二次副作用；故障以剧本表达（`timeout_once_then_success` 等），同一输入永远同一轨迹。见 `README.md`「Mock Commerce API」段。
6. **行为回归 harness（用例是数据）**：21 条 E2E 场景是一张数据表（输入→期望轨迹），Runner 驱动完整链路后与 baseline 场景指纹对比，**通过率退化即非零退出**，在 CI 语义下可拦截。见 `README.md`「行为验证体系」段。

**可迁移到主项目（这是全部 18 个里迁移价值最高的）**：
- **错误四分类**：主项目 `api/retry_policy.py` 已有重试，可升级为分类驱动（尤其 429/503/超时 vs 上游业务错误码），把「重试是分类驱动的决策」落地。
- **幂等键**：主项目写操作（如「重置号池」「编辑租约释放」）加幂等键，配合 SQLite 唯一索引去重。
- **PreToolUse 模式门 + HITL**：主项目「生产收紧」类操作（真实付费调用、生产配置变更）可加 `READONLY/ASK/EXECUTE` 门，付费 API 预算=0 红线正好用 READONLY 兜底。
- **两阶段故障注入**：主项目 Mock 上游（预算=0 红线要求 Mock 验证）可加「落库后超时」剧本，端到端验证重试/超时语义。
- **一份事件流多消费方**：主项目 SSE 已是事件流，可对齐「事件上增补元数据（trace_id/attempt/duration_ms）而非新设计」。

**主项目已有或更好（不迁移）**：任务队列/SSE（EcomAgent 无持久化队列，主项目 SQLite worker 更强）；MCP 客户端（超出主项目范围）。

---

### 8. commerce-agents

**一句话定位**：Anthropic 官方的 Claude 电商双 Agent 参考实现（shopping agent + merchant agent），定义一次、三套运行（Messages API / Agent SDK / Managed Agents），全部写操作 staged 待人工批准。

**技术栈**：Python 3.11 / Node 22 / Claude SDK / FastAPI 示例（examples 8 个 web app）/ Claude Code 插件形态（commerce-builder）。

**最有价值设计点**：

1. **Fence 清洗（fencing.py）——防提示注入的工业级实现**：把不可信文本按不可见 Unicode（零宽字符/bidi/变体选择符）、C0/C1 控制符、伪造角色边界（`human/assistant/system/user:`）、仿 transcript/tool 标签全部剥离到 fixpoint，`max_chars` 边界与截断后缀一起算，`sanitize_value` 递归清洗 dict/list——**在把数据喂给模型前先清洗**。见 `commerce-common/commerce_common/fencing.py:20-149`。
2. **staged change + 双 actor 门禁**：merchant agent 的每次写操作先 `stage_change` 落变更计划，`apply_change` 必须核对 `change_id` 是本会话 staged 且已 `approved_change_ids`，否则 BLOCK；`check_listing_provenance/check_campaign_provenance` 强制内容编辑先做 `get_listing` 读（provenance 门）。见 `merchant-agent/core/merchant_agent/gates.py:113-228`。
3. **grounding 规则**：`GroundingRule` 与 `matches_terms_and_cues` 做「工具级路由提示」——模型该走哪条工具路径有可测的确定性规则兜底。见 `commerce-common/commerce_common/grounding.py:22-75`。
4. **运行时统一**：`commerce_common` 的 turn/execution/streaming 抽成共用层，两 agent 都跑同一执行框架；`config.py` 提供环境驱动配置。

**可迁移到主项目**：
- **Fence 清洗层**：主项目如果让 LLM 读取上游返回/用户输入再二次生成提示词，这层清洗（尤其零宽字符/bidi/伪造 `<tool_result>` 标签）是硬需求——防提示注入，成本极低、价值极高。`fencing.py:20-149` 可直接移植为 `api/utils/` 模块。
- **staged change 门禁**：主项目「编辑租约释放」「号池重置」等管理写操作可先落「变更计划」再执行，带 provenance 检查。
- **grounding 确定性规则**：主项目 adaptive_router 的 MAB 之外，可加一层确定性「工具路径路由规则」兜底。

**主项目已有或更好（不迁移）**：agent 运行时本身（主项目不是 agent 框架）；三运行形态（超范围）。

---

### 9. CommerceAgentBench

**一句话定位**：阿里国际 Accio 出的有状态电商 Agent 基准——107 个任务（53 CLI/28 浏览器/16 文件/10 API-MCP），每个任务跑在全新容器，由确定性或 LLM 辅助 verifier 判分。

**技术栈**：Python 3.11 / Docker（每任务容器）/ mock_services 本地复刻 SaaS 系统 / LLM judge / OpenClaw harness。

**最有价值设计点**：

1. **verifier 抽象与「原子确定性检查」**：`task_grader.py` 是共享 host-side grader——加载每任务 `_grader.py` 的 `grade()`，产出 `validation_checks[]`（每条 `{id, passed, reason, check_type}`），配合 `rubric.minimum_pass_score`。见 `bench_core/verifiers/task_grader.py:1-29`、`bench_core/verifiers/shopify_admin_v2.py:31-46`（`_check(check_id, passed, reason)` 辅助函数）。
2. **v2 评分：raw check list 就是 source of truth**：v1 在 raw check 上叠框架 meta-check 导致报告总是「x/3」；v2 改为「每条必过 + verifier 完成即可信」，`passed = total>0 且全过且 verifier_completed`。见 `bench_core/reward.py:118-183`。
3. **LLM judge 作为 verifier 之一**：`llm_judge.py` / `llm_judge_cli.py`，判分来源可混用（script_verifier / llm_rubric），但二进制最终奖励以「必过检查全过」为准。见 `bench_core/reward.py:53-114`。
4. **有状态评估**：mock_services 复刻 alibaba_publish/shopify_admin/stripe_cli/notion_cli 等真实系统，agent 必须「操作界面并改变状态」而非问答。见 `bench_core/mock_services/registry.py`、`real_replica_bench/mock_services/`。

**可迁移到主项目**：
- **原子 check 的 verifier 契约**：主项目若给「上游接入/新增 provider」建验收，可让每个 provider 自带 `_grader` 式校验（`check_id/passed/reason/check_type` 数组），配合 Mock 上游做 CI 门禁。
- **「verifier 完成才算数」的评分纪律**：任何「测试通过」声明都要求 verifier 真正跑完，避免框架 meta-check 掩盖真实覆盖。
- **LLM judge 混合判分 + 二进制门禁**：适合主项目「生成结果质量」的抽检（LLM 评分辅助 + 硬性检查为门）。

**主项目已有或更好（不迁移）**：pytest 单测/集成门禁体系（主项目已有 1742 基线）；Docker 容器化评测（主项目有 docker-compose 但不需要每任务容器）。

---

### 10. claude-ads

**一句话定位**：Claude-first 的付费广告运营系统——把授权导出/账号读入变成有证据的审计、计划、创意、监控、报告，默认只读，实时变更必须过批准/幂等/验证/审计/回滚五重门。

**技术栈**：Python / Claude Code Skill 形态 + `claude_ads_core/` Python 包 / JSON schema 契约 / 12 平台适配。

**最有价值设计点**：

1. **默认只读 + mutation_authority 五重门**：产品状态文档强制 `default_mutation_mode: read-only`；`_validate_mutation` 校验 mutation-plan 文档；`mutation_authority` 枚举 `none/draft-only/approved-plan-required`（或 `repository-only/draft-external/approved-external`）控制能否真正执行。见 `claude_ads_core/product_status.py:195-197`、`claude_ads_core/workflow_contracts.py:505-573`、`workflow_models.py:34`。
2. **契约驱动的运行工件**：每个 run 产出 schema 校验过的 `run_manifest`（含 privacy_class、data_lifecycle、evidence_policy、verification、recovery 字段），报告从规范化 JSON bundle 渲染而非临时拼装。见 `claude_ads_core/workflow_contracts.py:548-573`、`claude_ads_core/contracts.py:142-170`。
3. **contracts.py 手工校验器 + schema 双保险**：`_require_*` 手写校验函数 + `validate_contract(name, payload)` 注册表，对每类工件（finding/run/bundle）有独立校验函数，错误带 `$.路径` 定位。见 `claude_ads_core/contracts.py:51-309`。
4. **worker 有界 + 不写终稿**：「只 fan out 独立工作，给每个 worker 有界 scope，worker 产出 schema-valid findings，永不写最终报告」。见 `ads/SKILL.md:22-27`。
5. **完成声明必须可溯源**：`SKILL.md` operating order 第 9 条「用工具结果验证产物再声称完成」、第 10 条「结尾带 owners/next actions/measurement windows/rollback notes」。

**可迁移到主项目**：
- **默认只读 + mutation 五重门**：主项目「生产收紧」类接口（真实付费调用、上线配置）照此加 `mutation_authority` 三级枚举，配合付费 API 预算=0 红线天然一致。
- **schema 校验的 run manifest**：主项目每次「provider 接入/配置变更」落一个带 privacy_class/evidence 的 manifest，报告从它渲染。
- **worker 有界 + 不写终稿**：主项目若做多 worker 并行生成，规定 worker 只产出中间结果，终稿由编排层合成。

**主项目已有或更好（不迁移）**：广告业务逻辑本身（不相关）；Skill 形态（主项目是服务不是 agent skill）。

---

### 11. 1click-ecom-detailpage

**一句话定位**：电商主图+详情页（PDP）一键生成 Skill——先诊断转化驱动力，再写英文基准文案、锁定 Campaign 风格、逐图过 Image Copy Gate 后出可执行 Prompt 或直接生图。

**技术栈**：Python 3.10+（`scripts/generate_image.py` 单文件 OpenAI 兼容生图脚本）/ Agent Skill 形态。

**最有价值设计点**：

1. **Conversion Driver Diagnosis 先行**：先判定 Visual-Driven / Pain-Driven / Emotion-Value-Driven 三类转化驱动力，再决定图/文案结构——「Every block must serve conversion. Remove decorative sections.」。见 `SKILL.md`「Core Principle」段。
2. **Minimum Input Resolution Gate 权重链**：输入收集按 `用户输入/附件 > 用户追问 > 联网研究 > 逻辑推理 > 假设/默认` 严格分级，高权重覆盖低权重，缺关键字段先问不问就猜；事实类只采用可追溯来源。见 `SKILL.md`「Minimum Input Resolution Gate」段。
3. **多图一致性：Campaign Style Lock**：多图任务先锁风格/色板/光线/布局/产品呈现，整套图包视觉统一。见 `SKILL.md` workflow 第 7 条。
4. **Image Copy Gate**：每张图先定图内短文案再写独立 Prompt——图内文字与视觉分离设计。见 `SKILL.md` workflow 第 9 条。
5. **Self-review Scorecard 自审门**：自审未达标先重写再输出。

**可迁移到主项目**：
- **生成前诊断 + 输入权重链**：主项目「文生图」若面向营销场景，可在提示词生成前加「转化驱动力诊断」与「输入缺失先问」的流程，减少无效生成（符合预算红线）。
- **Campaign Style Lock**：多图任务（如 5 张主图）先锁全局风格参数再逐图生成——主项目可在任务参数里加 `style_lock` 字段。
- **Image Copy Gate**：图内文字作为独立字段生成后再并图，主项目 txt2img 可加「图内文案校验」环节。

**主项目已有或更好（不迁移）**：生图 API 适配（主项目多 provider 更强）；电商详情页业务逻辑（不相关）。

---

### 12. prompt-craft-ecommerce-visuals

**一句话定位**：电商视觉提示词库（prompt templates 集合，单 HTML 分发），为商品图生成提供平台化/季节性/情绪化提示词框架。

**技术栈**：纯文档/HTML（index.html 为空，实际内容是 GitHub Pages 分发的静态站点）+ YAML 品牌画像示例。

**最有价值设计点**：

1. **品牌画像 yaml 驱动**：`brand-profile.yaml` 定义视觉身份（primary/secondary palette、typography_mood、composition_style、target_demographic、product_categories + emotional_triggers）——把「品牌一致性」参数化。见 `README.md`「Example Profile Configuration」段。
2. **平台规格敏感**：按 Instagram Carousel / Amazon listing / Shopify gallery / Pinterest pin 区分提示词格式。
3. **季节性/文化上下文层**：提示词模板适配节假日与目标市场文化。

**可迁移到主项目**：
- **品牌画像参数化**：主项目可在任务参数里加 `brand_profile` 结构（色板/风格/受众），provider 拼 prompt 时注入——适合主项目未来「电商视觉」场景。

**主项目已有或更好（不迁移）**：几乎全部——它是纯提示词资产，无工程可迁移；价值仅在提示词文案本身（超范围）。

---

## 任务 H — Captcha 类

---

### 13. cf-turnstile-token

**一句话定位**：单文件 Python CLI，把「读页面 sitekey → 提交 Peak 求解 API → 拿回 cf-turnstile-response token」流程脚本化，零浏览器依赖。

**技术栈**：纯 Python 标准库（urllib/argparse），185 行单文件，外部 Peak API。

**最有价值设计点**：

1. **sitekey 自动提取**：`data-sitekey="..."` + `sitekey/render:` 双正则，输入页面 URL 自动找 sitekey。见 `cf_turnstile_token.py:35-36`、`cf_turnstile_token.py:58-64`。
2. **求解请求 payload 规范**：`{task_type: "turnstiletask", sitekey, url, proxy?, action?, cdata?}`，proxy/action/cdata 都是可选项。见 `cf_turnstile_token.py:67-82`。
3. **重试退避**：3 次尝试、`time.sleep(2*attempt)` 线性退避、带 error 信封（`PeakError`）。
4. **结构化输出**：plain token 或 JSON 两种输出，适合管道。

**可迁移到主项目**：
- **sitekey 自动提取**：主项目 cf_solver 目前大概要手动传 sitekey；加一个「从目标页面自动提取 sitekey」的辅助函数即可。
- **求解 API payload 规范**：主项目 cf_solver 若对接第三方求解 API，照此 payload 契约（proxy/action/cdata 可选字段）。

**主项目已有或更好（不迁移）**：求解器本身（主项目有自建 cf_solver 联邦 + 熔断，远比调单点 API 强）；重试（主项目 retry_policy 更完整）。

---

### 14. CloudFlareInvisibleSolver

**一句话定位**：逆向 Cloudflare jsd 挑战（`/cdn-cgi/challenge-platform/h/b/jsd/oneshot`）的 Python 求解器，用 requests 复现浏览器挑战流程换取 `cf_clearance` cookie。

**技术栈**：Python / requests / 自实现 deflate 压缩 + 自定义 base64 + FNV1a+xorshift XOR 加密（decode_p.py）。

**最有价值设计点**：

1. **协议逆向的纯 Python 复现**：`decode_p.py` 完整实现自定义 base64（自定义 alphabet）、FNV1a/XOR keystream、deflate 解压、帧头解析（magic 0xFD 0x01 + flag）——证明「无浏览器也能解 jsd 挑战」。见 `CloudFlareInvisibleSolver/decode_p.py:1-95`。
2. **alphabet 从 JS 码表注入**：`E_ALPHABET` 标注「改为 jsd 文件对应的码表」，即从目标 JS 提取 alphabet 驱动解/加密——加密参数外部化。见 `decode_p.py:3-4`。
3. **完整请求链模拟**：`CloudFlareInvisible` 类按 UA→拉 main.js→构造 payload→`oneshot/{jsd_url_path}/{ray}` POST→收 `cf_clearance` cookie 的顺序模拟浏览器。见 `CloudFlareInvisibleSolver/test.py:201-2015`。

**可迁移到主项目**：
- **协议逆向思路参考**：若主项目 cf_solver 想减少对 Playwright 的依赖，可参考此项目「纯 HTTP 复现 jsd 挑战」的路径（但该路径脆弱且随 Cloudflare 更新失效，仅作思路）。

**主项目已有或更好（不迁移）**：**几乎全部**——主项目自建 cf_solver（boterdrop_wrapper）已是生产级多节点联邦+熔断；此项目是脆弱的单点逆向（jsd 协议更新即失效），且用 requests 硬编码 UA 易被检测。不建议迁移其代码，只可借鉴其「挑战协议拆解」思路。

---

### 15. ohmycaptcha

**一句话定位**：自托管、YesCaptcha 风格 API 的验证码求解服务（FastAPI + Playwright），19 种任务类型，reCAPTCHA/hCaptcha/Turnstile/图像分类全覆盖。

**技术栈**：Python / FastAPI / Playwright（浏览器自动化）/ 本地 Qwen3.5-2B（SGLang）或云端 OpenAI 兼容模型做图像分类 / Render 部署。

**最有价值设计点**：

1. **YesCaptcha/2Captcha 兼容 API 面**：`/createTask → {taskId}`、`/getTaskResult → {status: processing|ready, solution}`、`/getBalance`，三端点就是行业标准异步求解协议。见 `src/api/routes.py:65-157`、`src/models/task.py`（CreateTask/GetTaskResult/GetBalance 全部 pydantic 模型化）。
2. **TaskManager：async 内存队列 + TTL 清理**：`create_task` 生成 uuid、`asyncio.create_task` 后台处理、状态机 PROCESSING/READY/FAILED、10 分钟 TTL 自动过期删除。见 `src/services/task_manager.py:38-89`。
3. **Solver Protocol 注册表**：`Solver(Protocol): async def solve(params)`，`register_solver(task_type, solver)` 按类型注册；TurnstileSolver 内部 Playwright 打开页面 → 点 checkbox → 页面 evaluate JS 提取 token。见 `src/services/task_manager.py:34-36`、`src/services/turnstile.py:44-142`。
4. **前端免费/云模型切换**：图像分类可选本地 SGLang 模型或云端 OpenAI 兼容，通过环境变量切换（与预算红线兼容）。

**可迁移到主项目**：
- **YesCaptcha 兼容协议面**：主项目 cf_solver 若对外暴露求解服务，照 `createTask/getTaskResult` 异步协议（兼容 2Captcha 生态），比自定义协议更通用。
- **Solver Protocol 注册表**：主项目 solver_guard 联邦可加一层「按任务类型注册求解器」的协议抽象。
- **异步内存任务 + TTL**：轻量级求解任务可直接复用此模式（主项目重型任务走 SQLite，但 token 预取这类轻任务可用内存 TTL 任务）。

**主项目已有或更好（不迁移）**：浏览器求解细节（主项目 cf_solver 已有）；TaskManager（主项目 SQLite+SSE 更强）；图像分类（超出范围）。

---

### 16. 2Captcha-MCP

**一句话定位**：把完整 2Captcha API 面（43 个工具）暴露给 Claude Code 的 MCP 服务器——31 求解 + 5 管理 + 3 pingback CRUD + 3 webhook 事件存储 + 1 复合「提交并等结果」工具。

**技术栈**：Python 3.11+ / MCP SDK / 官方 `2captcha-python` SDK / pydantic-settings / SQLite webhook 存储。

**最有价值设计点**：

1. **AliasChoices 环境变量别名 + 校验**：pydantic-settings 的 `AliasChoices("2Captcha_API_KEY", "TWOCAPTCHA_API_KEY", ...)` 多别名兼容 + 超时/轮询/限流全字段带 `ge/le` 边界 + `field_validator` 归一化。见 `twocaptcha_mcp/config.py:20-130`。
2. **pingback 本地落库 + 复合等待工具**：`solve_and_wait_pingback` 提交时带 pingback URL，本地 webhook receiver 收结果写 SQLite（`pingback_events` 表 + 唯一约束去重），工具轮询本地 store 而非阻塞上游——「回调优先于轮询」的降延迟模式。见 `twocaptcha_mcp/tools/composite.py:40-60`、`twocaptcha_mcp/webhook_receiver/storage.py:17-120`。
3. **webhook 签名校验**：`signature.py` 的 `verify_secret` 防伪回调。
4. **工具/schema 按类型分文件**：`tools/` 与 `schemas/` 一一对应（amazon/atb/composite/funcaptcha/geetest/hcaptcha/image/...），`registry.py` 注册 43 工具。

**可迁移到主项目**：
- **AliasChoices 多别名配置**：主项目 `IF_*` 前缀已有，可为兼容旧环境变量加别名。
- **回调落库 + 轮询本地**：主项目 SSE replay 其实已是同类思想，可参考其「回调写入 SQLite 唯一约束去重」的落库方式。
- **webhook 签名校验**：主项目若暴露 webhook 端点接收上游回调，加 `verify_secret` 式 HMAC/secret 校验。

**主项目已有或更好（不迁移）**：求解调用（主项目 cf_solver 联邦）；SSE 事件流（主项目更完整）；MCP 形态（超范围）。

---

### 17. captcha-solver

**一句话定位**：本地、零外部费用的验证码求解 HTTP sidecar（FastAPI :8877）——基于 CloakBrowser（anti-detect Chromium）原生跑挑战，11 种类型全 harvest-only。

**技术栈**：Python / FastAPI / CloakBrowser / Playwright / ONNX（reCAPTCHA 图像分类）/ Mistral 视觉 API（可选）/ Xvfb（headed recaptcha）。

**最有价值设计点**：

1. **统一 `/solve` 端点 + 类型分发**：按 `type` 分发到各 sub-solver，统一响应带顶层 `solved` bool——「读 solved，不要按类型分支」。见 `server.py:538-573`。
2. **错误契约两条规则**：solve 跑了但没成功 → 200 + `solved:false` + error；4xx/5xx → 表示请求根本没解决（FastAPI detail 信封）——**永不两种同时出现**。见 `server.py:569-572`。
3. **pre_actions / post_fetch 脚本化**：求解前可执行 UI 步骤（click/fill/select/press/wait，支持 CSS/XPath/text=/role= 选择器），求解后可从**同一浏览器会话**发 post_fetch（用 `__TOKEN__` 占位符注入 token）——解决「token 与 cookie/UA/IP 绑定」的重放问题。见 `server.py:153-172`、`server.py:217-220`。
4. **solve-and-verify（Turnstile）**：同一会话内先解 token 再调 `verify_url` 验证（`payload["token"]=token`），token 是 300s 单次有效，验证不过不返回。见 `turnstile/solve.py:87-119`。
5. **SSRF 防护 + 全局超时**：`_assert_public_url` 拒绝内网地址（`_validate_urls`），每请求 deadline 到期返回 408 并释放浏览器——防挂起锁死 per-type lock。见 `server.py:117-151`、`server.py:605-607`。
6. **「harvest-only」边界**：明示本服务只解验证码+收 token，账号创建/表单侦查是调用方职责——职责切割清楚。

**可迁移到主项目（全部 captcha 参考里迁移价值最高）**：
- **统一 solved 响应 + 双规则错误契约**：主项目 cf_solver 对外接口可统一 `solved: bool` 顶层字段，错误用「运行过但失败 vs 根本没解」两套语义，消除下游分支混乱。
- **pre_actions/post_fetch 脚本化**：主项目 Turnstile token 预取池（token_pool.py）可在求解时带 post_fetch（同一会话注入 token 调目标接口），提升 token 可用率。
- **同一会话 solve-and-verify**：token 是单次 300s 有效，主项目预取池应在同一浏览器会话内验证而非裸取，避免拿到已失效 token。
- **SSRF 防护**：主项目任何接受 URL 的端点照 `_assert_public_url` 加内网地址拒绝。
- **harvest-only 职责切割**：主项目 solver_guard 与号池职责已类似，可显式文档化边界。

**主项目已有或更好（不迁移）**：浏览器求解细节（cf_solver 已有）；图像分类（超范围）。

---

### 18. Antibot-Detector

**一句话定位**：Scrapfly 的 Manifest V3 Chrome 扩展——实时识别页面上的反爬系统（16 种 anti-bot）、验证码（8 种 captcha）、指纹采集技术（21 种 fingerprint）。

**技术栈**：JavaScript/Chrome Extension MV3 / JSON 规则文件（detectors/*.json）/ background+content script。

**最有价值设计点**：

1. **检测规则全 JSON 数据驱动**：每个 anti-bot/captcha 系统一个 JSON 文件，字段含 `confidence`（证据置信度）、检测维度 `cookie/header/url/content`，每维度条目带 `name/value/text + Regex/WholeWord/CaseSensitive + Scope`。见 `detectors/antibot/detect-cloudflare.json:1-65`、`detectors/captcha/detect-hcaptcha.json:1-50`。
2. **多证据累加置信度**：一个系统可由多条低置信证据（如 __cf_bm cookie=50 + challenge URL=90 + cf_clearance=100）共同命中，索引 `detectors/index.json` 分组管理。
3. **「URL/域名特征 → 系统识别」**：许多检测是纯 URL/域名匹配（`hcaptcha.com`、`/checksiteconfig`、`/getcaptcha`），这为服务端侧「识别目标页面挂的是什么验证码」提供了廉价特征库。

**可迁移到主项目**：
- **检测规则数据驱动**：主项目可维护一个 `detectors/` JSON 库（按主项目技术栈用 YAML/Python dict），用于**自动识别目标上游页面挂了什么验证码/反爬**（如抽到 Cloudflare 就用 cf_solver，抽到 reCAPTCHA 走 2captcha 类），配合 solver_guard 联邦路由。
- **置信度证据模型**：`{signal, confidence, scope}` 的累加判定适合做成 solver 路由决策打分（与主项目 MAB-EWMA 融合）。

**主项目已有或更好（不迁移）**：Chrome 扩展形态本身（超范围，主项目是服务端）；具体检测脚本（主项目按需取用特征而非装扩展）。

---

## 汇总：跨项目可提炼的通用设计思想（按迁移优先级）

| 优先级 | 思想 | 来源项目 | 主项目落点 |
|---|---|---|---|
| P0 | **错误四分类驱动重试**（CLIENT/BUSINESS 不重试，TRANSIENT 退避） | EcomAgent `execution/error_classification.py:13-80` | 升级 `api/retry_policy.py` |
| P0 | **统一 solved 响应 + 双规则错误契约**（跑过但失败 vs 根本没解） | captcha-solver `server.py:569-572` | cf_solver 对外接口 |
| P0 | **Fence 清洗层**（零宽字符/bidi/伪造标签剥离到 fixpoint） | commerce-agents `fencing.py:20-149` | 新增 `api/utils/`，LLM 读上游数据前清洗 |
| P0 | **同一会话 solve-and-verify**（token 单次 300s） | captcha-solver `turnstile/solve.py:87-119` | token_pool.py 预取池 |
| P1 | **参数矩阵 schema 化**（60+ 参数显式 Pydantic + 枚举内聚分辨率映射） | MoneyPrinterTurbo `schema.py:91-161` | 统一 GenerationParams + provider 适配层 |
| P1 | **失败阶段落库**（failed_stage 三件套） | MoneyPrinterTurbo `schema.py:289-306` | 任务表加 failed_stage |
| P1 | **结构化进度事件**（event_type/progress/step/action） | Pixelle-Video `progress.py:23-63` | 升级 SSE 进度事件 |
| P1 | **staged change + provenance 门禁** | commerce-agents `gates.py:113-228` | 管理写操作（租约/号池） |
| P1 | **默认只读 + mutation_authority 三级** | claude-ads `workflow_contracts.py:505-573` | 生产收紧接口 + 付费红线兜底 |
| P1 | **幂等键**（写操作 sha256 参数哈希 + 只读自动跳过） | EcomAgent `idempotency.py:13-62` | 写操作 + SQLite 唯一索引 |
| P1 | **两阶段故障注入**（副作用落库后才超时） | EcomAgent README Mock 段 | Mock 上游剧本 |
| P1 | **LLM 结构化输出三件套**（宽松解析→白名单清洗→重试+纠正提示） | AI-Youtube-Shorts `highlights.py:98-248` | 任何 LLM 生成环节 |
| P2 | **pre_actions/post_fetch 脚本化** | captcha-solver `server.py:153-172` | 求解任务脚本化 |
| P2 | **Sitekey 自动提取** | cf-turnstile-token `cf_turnstile_token.py:58-64` | cf_solver 辅助 |
| P2 | **原子 check verifier 契约**（check_id/passed/reason/check_type） | CommerceAgentBench `task_grader.py:1-29` | provider 接入验收 |
| P2 | **回调落库 + 轮询本地 + 签名校验** | 2Captcha-MCP `composite.py:40-60` | webhook 端点 |
| P2 | **Anti-bot 检测规则数据驱动 + 置信度证据** | Antibot-Detector `detectors/` | solver 路由决策 |
| P2 | **流水线异常兜底 + 入队失败回滚** | MoneyPrinterTurbo `task.py:1585-1616` | worker 队列 |
| P3 | **Pipeline 抽象**（BasePipeline + core 服务） | Pixelle-Video `pipelines/base.py:28-93` | 新链路扩展 |
| P3 | **独立终检 Agent 清单** | video-shotcraft `final-review.md:1-116` | 成品抽检 |
| P3 | **配置优先级链 + 多别名 + 掩码纪律** | VideoCaptioner/2Captcha-MCP | config 层 |
| P3 | **style_lock / brand_profile 参数化** | 1click-ecom/prompt-craft | 电商视觉场景 |
| — | **不迁移**：Remotion 渲染、Workbench、TTS 权重、协议逆向（CloudFlareInvisibleSolver）、纯提示词库、Chrome 扩展形态 | — | — |

**一句话总结**：EcomAgent（错误分类+幂等+HITL）、captcha-solver（统一协议+同一会话验证+脚本化）、commerce-agents（Fence 清洗）、MoneyPrinterTurbo（参数矩阵 schema 化）是四个迁移价值最高的项目；CloudFlareInvisibleSolver 与 prompt-craft-ecommerce-visuals 基本无可迁移工程资产。
