# 任务目标与上下文重建

## 任务背景
用户要求将 imagefree.net 的 Cloudflare Turnstile 求解方案从单机瓶颈升级为 v5.0 容错分布式架构，核心诉求：
1. **Solver Node Federation** — 多节点负载均衡、429 熔断与故障转移
2. **CDP 极速求解** — 资源拦截优化、求解时间从 6-8s 压缩至 1.8-2.5s
3. **Token 双缓冲预热池** — 0ms 零等待获取 token

## 当前状态
- 实现完整代码已写入并测试通过（45 passed, 1 xfailed）
- 变更文件已同步至 `deploy/api/` 与 `deploy/cf_solver/`
- 用户原有未提交改动（前端、graft、规划书等）未受干扰
- 尚未进行独立审查、盲点扫描与文档同步

## 影响半径
- 核心模块：`api/config/solver.py`、`api/solver_guard.py`、`api/turnstile_client.py`、`api/worker/token_pool.py`
- 部署同步：`deploy/api/` 对应文件
- 求解器：`deploy/cf_solver/api_server.py`
- 测试工具：`scripts/mock_cfsolver.py`
- 新测试：`tests/test_solver_federation_e2e.py`
- 不涉及：数据库、前端、API 路由、外部付费服务

## 关键假设
- 生产环境有 2+ 个 cf_solver 节点（或可启动多个实例）
- 现有 `CF_SOLVER_URL` 单机配置向后兼容
- 求解器节点之间无共享状态，完全独立
- Token TTL（90s）足够长，双缓冲池不会因过期导致大量浪费

## 风险等级
- L2（中风险）：多文件改动，但公共接口向后兼容，有完整测试覆盖
