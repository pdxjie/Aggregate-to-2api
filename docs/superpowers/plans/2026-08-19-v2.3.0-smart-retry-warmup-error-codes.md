# v2.3.0 智能重试 + 缓存预热 + 错误码分层 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 实现 F-04 自适应重试策略、F-05 缓存预热、Q-02 错误码分层体系

**架构：** 三个独立任务并行开发，各自有测试覆盖，最后合并到 main 并通过 E2E 验收

**技术栈：** Python 3.12, FastAPI, pytest, SQLite

**涉及文件：**
- `api/retry_policy.py` — F-04 重写
- `api/worker.py` — F-04 使用新策略
- `api/errors.py` — Q-02 重写
- `api/main.py` — F-05 + Q-02 调整
- `tests/test_retry_policy.py` — F-04 测试更新
- `tests/test_errors.py` — Q-02 新增测试
- `deploy/api/` — 同步目标

---

### 任务 1：F-04 — AdaptiveRetryStrategy 自适应重试

**文件：**
- 修改：`api/retry_policy.py`
- 修改：`api/worker.py:608-676`
- 修改：`tests/test_retry_policy.py`

- [ ] **步骤 1：编写 AdaptiveRetryStrategy 测试**

```python
# tests/test_retry_policy.py 新增测试类
class TestAdaptiveRetryStrategy:
    def test_classify_rate_limited(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.classify("429 Too Many Requests") == "rate_limited"
        assert strategy.classify("rate limit exceeded") == "rate_limited"

    def test_classify_timeout(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.classify("timeout") == "timeout"
        assert strategy.classify("connection timed out") == "timeout"

    def test_classify_token_rejected(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.classify("token_rejected") == "token_rejected"
        assert strategy.classify("human verification failed") == "token_rejected"

    def test_classify_server_error(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.classify("503 Service Unavailable") == "server_error"
        assert strategy.classify("500 Internal Server Error") == "server_error"

    def test_classify_network_error(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.classify("ConnectionError") == "network_error"
        assert strategy.classify("connection refused") == "network_error"

    def test_classify_permanent_as_unknown(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.classify("422 Unprocessable Entity") == "permanent"

    def test_delay_rate_limited_attempt_1(self):
        strategy = AdaptiveRetryStrategy()
        d = strategy.delay(1, "rate_limited")
        assert 0 <= d <= 60  # backoff_base=30 * 2^0 = 30, jitter=0.5 → max 60

    def test_delay_max_cap(self):
        strategy = AdaptiveRetryStrategy()
        d = strategy.delay(100, "rate_limited")
        assert d <= 300  # max 300

    def test_retry_after_header(self):
        strategy = AdaptiveRetryStrategy()
        d = strategy.delay_from_retry_after("Retry-After: 120")
        assert d == 120
        d = strategy.delay_from_retry_after("Retry-After: 5")
        assert d == 5

    def test_should_retry_transient(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.should_retry(1, 3, "timeout") is True
        assert strategy.should_retry(3, 3, "timeout") is False

    def test_should_retry_permanent(self):
        strategy = AdaptiveRetryStrategy()
        assert strategy.should_retry(1, 3, "422") is False
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pytest tests/test_retry_policy.py -v`
预期：FAIL，`AdaptiveRetryStrategy` 未定义

- [ ] **步骤 3：重写 retry_policy.py**

```python
"""IMP-05: 重试策略 - 自适应错误分类、指数退避 + jitter + Retry-After 支持。

为 worker 提供统一的错误分类和自适应退避计算。
支持基于错误类型配置不同退避策略和最大重试次数。
"""
import random
import re
import time
from typing import ClassVar


class AdaptiveRetryStrategy:
    """自适应重试策略：根据错误类型和上游反馈动态调整退避。

    ERROR_TYPES 定义每种错误类型的退避基值、最大重试次数、jitter 系数。
    delay() 执行指数退避：delay = min(backoff_base * 2^(attempt-1), max_delay)
    """

    ERROR_TYPES: ClassVar[dict[str, dict]] = {
        "rate_limited": {"backoff_base": 30, "max_retries": 5, "jitter": 0.5},
        "token_rejected": {"backoff_base": 5, "max_retries": 3, "jitter": 0.2},
        "timeout": {"backoff_base": 10, "max_retries": 3, "jitter": 0.3},
        "server_error": {"backoff_base": 15, "max_retries": 4, "jitter": 0.4},
        "network_error": {"backoff_base": 5, "max_retries": 2, "jitter": 0.5},
    }

    MAX_DELAY = 300.0  # 所有退避上限 300 秒

    # 错误类型判定标记（按优先级）
    _RATE_LIMITED_MARKERS: ClassVar[tuple[str, ...]] = (
        "429", "rate_limit", "rate limit", "too many requests",
    )
    _TOKEN_REJECTED_MARKERS: ClassVar[tuple[str, ...]] = (
        "token_rejected", "human verification failed",
    )
    _TIMEOUT_MARKERS: ClassVar[tuple[str, ...]] = (
        "timeout", "timed out",
    )
    _SERVER_ERROR_MARKERS: ClassVar[tuple[str, ...]] = (
        "5xx", "503", "502", "504", "500",
    )
    _NETWORK_ERROR_MARKERS: ClassVar[tuple[str, ...]] = (
        "connectionerror", "connection refused", "connection reset",
    )
    _PERMANENT_MARKERS: ClassVar[tuple[str, ...]] = (
        "422", "404",
    )

    # 兼容旧版 RetryPolicy 接口
    TRANSIENT_TYPES = frozenset(("rate_limited", "token_rejected", "timeout", "server_error", "network_error"))

    @classmethod
    def classify(cls, error: object) -> str:
        """基于错误消息和类型分类错误。

        返回值为 PERMANENT_MARKERS: permanent, 否则为 ERROR_TYPES 中的 key 之一。
        规则（按优先级，首个匹配为准）：
        - rate_limited: 429, rate_limit, too many requests
        - token_rejected: token_rejected, human verification failed
        - timeout: timeout, timed out
        - server_error: 5xx, 503, 502, 504, 500
        - network_error: ConnectionError, connection refused, connection reset
        - permanent: 422, 404
        - 默认: timeout（保守重试）
        """
        msg = str(error).lower()

        # 检查 httpx 响应状态码
        try:
            status = getattr(error, "response", None)
            if status is not None:
                status_code = getattr(status, "status_code", None) or getattr(status, "status", None)
                if status_code is not None:
                    if status_code == 429 or ("rate_limit" in msg or "rate limit" in msg):
                        return "rate_limited"
                    if status_code in (422, 404):
                        return "permanent"
                    if 500 <= status_code < 600:
                        return "server_error"
                    if status_code == 400 and ("rate_limit" in msg or "rate limit" in msg):
                        return "rate_limited"
        except (AttributeError, TypeError):
            pass

        # 字符串匹配
        for marker in cls._RATE_LIMITED_MARKERS:
            if marker in msg:
                return "rate_limited"
        for marker in cls._TOKEN_REJECTED_MARKERS:
            if marker in msg:
                return "token_rejected"
        for marker in cls._TIMEOUT_MARKERS:
            if marker in msg:
                return "timeout"
        if isinstance(error, (TimeoutError, ConnectionError)):
            return "network_error"
        for marker in cls._SERVER_ERROR_MARKERS:
            if marker in msg:
                return "server_error"
        if isinstance(error, ConnectionError):
            return "network_error"
        for marker in cls._NETWORK_ERROR_MARKERS:
            if marker in msg:
                return "network_error"
        for marker in cls._PERMANENT_MARKERS:
            if marker in msg:
                return "permanent"

        # 默认保守：timeout（可重试）
        return "timeout"

    @classmethod
    def should_retry(cls, attempt: int, max_retries: int, error: object) -> bool:
        """判断是否应重试。attempt 从 1 开始计数。

        兼容旧版接口：permanent 错误即使 attempt < max_retries 也不重试。
        """
        if attempt >= max_retries:
            return False
        return cls.classify(error) != "permanent"

    @classmethod
    def delay(cls, attempt: int, error_type: str) -> float:
        """指数退避 + jitter 延迟计算。

        attempt 从 1 开始计数（第 1 次重试）。
        delay = min(backoff_base * 2^(attempt-1), MAX_DELAY)
        最终结果 = delay + random(0, delay * jitter)
        """
        config = cls.ERROR_TYPES.get(error_type, cls.ERROR_TYPES["timeout"])
        base = min(config["backoff_base"] * (2 ** (attempt - 1)), cls.MAX_DELAY)
        return base + random.uniform(0, base * config["jitter"])

    @classmethod
    def delay_from_retry_after(cls, header_value: str | None) -> float | None:
        """解析 Retry-After 头，返回退避秒数。若值不合规则返回 None。"""
        if not header_value:
            return None
        # 尝试秒数
        try:
            return float(header_value.strip())
        except ValueError:
            pass
        # 尝试 HTTP-date（RFC 7231）
        try:
            from email.utils import parsedate_to_datetime
            dt = parsedate_to_datetime(header_value.strip())
            return max(0, (dt.timestamp() - time.time()))
        except Exception:
            return None

    # ── 兼容旧版 RetryPolicy 接口 ────────────────────

    @classmethod
    def classify_error(cls, error: object) -> str:
        """兼容旧版接口：返回 'transient' 或 'permanent'。"""
        return "permanent" if cls.classify(error) == "permanent" else "transient"

    @classmethod
    def backoff_delay(cls, attempt: int, base_delay: float) -> float:
        """兼容旧版接口：随机退避 [0, base_delay * 2^(attempt-1))。"""
        max_delay = base_delay * (2 ** (attempt - 1))
        return random.uniform(0, max_delay)


# 别名，兼容旧导入
RetryPolicy = AdaptiveRetryStrategy
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pytest tests/test_retry_policy.py -v`
预期：ALL PASS

- [ ] **步骤 5：更新 worker.py 使用新策略**

修改 `api/worker.py` 中 `_process` 方法，确保 `RetryPolicy.should_retry` 调用正常（导入已存在，只需要检查用法兼容性）。

- [ ] **步骤 6：运行现有测试全通过**

运行：`pytest tests/ -v --timeout=30`
预期：ALL PASS

- [ ] **步骤 7：Commit**

```bash
git add api/retry_policy.py tests/test_retry_policy.py
git commit -m "feat: F-04 自适应重试策略 AdaptiveRetryStrategy

- 按错误类型分类（rate_limited/token_rejected/timeout/server_error/network_error）
- 每类独立配置退避基值、最大重试次数、jitter 系数
- 支持 Retry-After 头解析
- 兼容旧版 RetryPolicy 接口
- 保留旧版测试全部通过"
```

---

### 任务 2：F-05 — 缓存预热

**文件：**
- 修改：`api/main.py:70-130`（lifespan）
- 新增：`api/cache_warmup.py`

- [ ] **步骤 1：编写 cache_warmup.py**

```python
"""F-05: 缓存预热 — 启动时从 DB 加载常见查询到缓存，减少重启后 DB 读压。"""
import logging

log = logging.getLogger("cache_warmup")


async def warmup_cache(gallery_cache, db) -> dict[str, int]:
    """启动后预热缓存：按优先级从 DB 加载常见查询，异步非阻塞。

    预热不影响启动速度（lifespan 中异步执行，不阻塞 yield）。
    返回 {key: count} 预热结果。
    """
    result: dict[str, int] = {}
    try:
        # 1. 统计概览（立即预热）
        overview = db.stats_overview()
        await gallery_cache.set("stats:overview", overview)
        result["stats:overview"] = 1
        log.info("缓存预热: stats:overview")
    except Exception as e:
        log.warning("缓存预热 stats:overview 失败: %s", e)

    try:
        # 2. 近日统计（14 天 + 12 月）
        daily = db.stats_daily(14)
        await gallery_cache.set("stats:daily:14", daily)
        result["stats:daily:14"] = 1
        monthly = db.stats_monthly(12)
        await gallery_cache.set("stats:monthly:12", monthly)
        result["stats:monthly:12"] = 1
        log.info("缓存预热: stats 条目")
    except Exception as e:
        log.warning("缓存预热 stats 失败: %s", e)

    try:
        # 3. 画廊（常见 limit 值）
        from . import config
        for limit in [10, 20, 50]:
            items = db.recent_images(limit)
            cache_key = f"gallery:{limit}"
            await gallery_cache.set(cache_key, {"items": items, "count": len(items)})
            result[cache_key] = 1
        log.info("缓存预热: gallery 条目")
    except Exception as e:
        log.warning("缓存预热 gallery 失败: %s", e)

    log.info("缓存预热完成: %d 个条目", len(result))
    return result
```

- [ ] **步骤 2：在 main.py lifespan 中调用**

在 `lifespan` 中 `gallery_cache.start_reaper()` 和 `restore_from_db()` 之后添加：

```python
    # F-05: 缓存预热（异步非阻塞，不阻塞 yield）
    _warmup_task = asyncio.create_task(warmup_cache(gallery_cache, db))
```

并在 yield 后清理：
```python
    if _warmup_task and not _warmup_task.done():
        _warmup_task.cancel()
```

- [ ] **步骤 3：验证启动时预热日志**

运行：`python -c "from api.cache_warmup import warmup_cache; print('import OK')"`
预期：import OK

- [ ] **步骤 4：Commit**

```bash
git add api/cache_warmup.py api/main.py
git commit -m "feat: F-05 缓存预热 — 启动时加载 stats/gallery 到缓存

- 启动时异步预热统计概览、近日统计、画廊常见 limit
- 不阻塞服务启动（lifespan 中异步协程）
- 预热失败不影响服务正常运行"
```

---

### 任务 3：Q-02 — 错误码分层体系

**文件：**
- 修改：`api/errors.py`
- 修改：`api/main.py:243-284`
- 新增：`tests/test_errors.py`

- [ ] **步骤 1：编写测试**

```python
# tests/test_errors.py
"""Q-02: 错误码分层体系测试。"""
import pytest
from api.errors import AppError, ErrorCodes, ErrorCategory, error_response, get_error_message


class TestErrorCategory:
    def test_category_values(self):
        assert ErrorCategory.AUTH in ("AUTH", "AUTH.001")
        assert ErrorCategory.VALIDATION in ("VAL", "VAL.001")

    def test_error_code_format(self):
        """错误码格式应为 CATEGORY.NNN。"""
        for code in ErrorCodes.__dict__.values():
            if isinstance(code, str) and "." in code:
                parts = code.split(".")
                assert len(parts) == 2
                assert parts[1].isdigit() and len(parts[1]) == 3


class TestAppError:
    def test_basic(self):
        err = AppError(ErrorCodes.QUEUE_FULL, "队列已满", status_code=429)
        assert err.code == "SYS.002"
        assert err.status_code == 429
        assert err.message == "队列已满"

    def test_with_details(self):
        err = AppError("PROV.001", "提供商不可用", details={"provider": "imagefree"})
        assert err.details["provider"] == "imagefree"

    def test_default_status_code(self):
        err = AppError("VAL.001", "参数错误")
        assert err.status_code == 400


class TestErrorMessage:
    def test_zh_message(self):
        msg = get_error_message("SYS.002", lang="zh", provider="测试")
        assert "繁忙" in msg

    def test_en_message(self):
        msg = get_error_message("SYS.002", lang="en")
        assert "busy" in msg

    def test_unknown_code(self):
        msg = get_error_message("UNKNOWN", lang="zh")
        assert msg  # 应有默认消息

    def test_fallback_to_default(self):
        msg = get_error_message("SYS.002", lang="ja")
        assert msg  # 回退到 en 或 zh


class TestErrorResponse:
    def test_error_response_format(self):
        resp = error_response("SYS.002", "系统繁忙", status_code=429)
        assert resp.status_code == 429
        body = resp.body
        if isinstance(body, bytes):
            import json
            body = json.loads(body)
        assert body["error"]["code"] == "SYS.002"
        assert body["error"]["message"] == "系统繁忙"
```

- [ ] **步骤 2：重写 errors.py**

```python
"""统一错误响应格式、分层错误码、多语言错误消息。

用法：
    raise AppError(ErrorCodes.QUEUE_FULL, "队列已满", status_code=429)
    return error_response("SYS.002", "系统繁忙", status_code=429)
    msg = get_error_message("SYS.002", lang="zh", provider="测试")
"""

from fastapi.responses import JSONResponse


class ErrorCategory:
    """错误码分类前缀（CATEGORY.NNN 格式）。"""
    AUTH = "AUTH"
    VALIDATION = "VAL"
    PROVIDER = "PROV"
    SYSTEM = "SYS"
    RATE_LIMIT = "RATE"


class ErrorCodes:
    """分层错误码常量定义，附 HTTP 状态码建议。

    格式：CATEGORY.NNN（分类.序号）
    """

    # 认证 (AUTH)
    UNAUTHORIZED = f"{ErrorCategory.AUTH}.001"              # 401 未授权
    API_KEY_EXPIRED = f"{ErrorCategory.AUTH}.002"            # 401 密钥过期

    # 校验 (VAL)
    INVALID_MODEL = f"{ErrorCategory.VALIDATION}.001"        # 422 模型不存在
    INVALID_PROMPT = f"{ErrorCategory.VALIDATION}.002"       # 422 提示词不符合要求
    INVALID_RATIO = f"{ErrorCategory.VALIDATION}.003"        # 422 比例格式错误
    BAD_REQUEST = f"{ErrorCategory.VALIDATION}.004"          # 400 通用错误

    # 提供商 (PROV)
    PROVIDER_DOWN = f"{ErrorCategory.PROVIDER}.001"          # 503 提供商不可用
    PROVIDER_OUT_OF_CREDITS = f"{ErrorCategory.PROVIDER}.002"  # 429 提供商额度耗尽
    SOLVER_CIRCUIT_OPEN = f"{ErrorCategory.PROVIDER}.003"    # 503 求解器熔断

    # 系统 (SYS)
    INTERNAL_ERROR = f"{ErrorCategory.SYSTEM}.001"           # 500 服务器内部错误
    QUEUE_FULL = f"{ErrorCategory.SYSTEM}.002"               # 429 队列满
    NOT_FOUND = f"{ErrorCategory.SYSTEM}.003"                # 404 资源不存在
    TASK_TIMEOUT = f"{ErrorCategory.SYSTEM}.004"             # 408 生成超时
    IDEMPOTENCY_KEY_EXISTS = f"{ErrorCategory.SYSTEM}.005"   # 409 幂等 Key 冲突
    RATE_LIMITED = f"{ErrorCategory.RATE_LIMIT}.001"         # 429 限流


# 多语言错误消息
ERROR_MESSAGES: dict[str, dict[str, str]] = {
    "zh": {
        ErrorCodes.PROVIDER_DOWN: "提供商 {provider} 暂时不可用，请稍后重试",
        ErrorCodes.PROVIDER_OUT_OF_CREDITS: "提供商 {provider} 额度耗尽",
        ErrorCodes.QUEUE_FULL: "系统繁忙，请稍后重试",
        ErrorCodes.UNAUTHORIZED: "认证失败",
        ErrorCodes.INVALID_MODEL: "未知模型: {model}",
        ErrorCodes.INVALID_PROMPT: "提示词不符合要求",
        ErrorCodes.INVALID_RATIO: "不支持的图片比例: {ratio}",
        ErrorCodes.BAD_REQUEST: "请求参数错误: {detail}",
        ErrorCodes.INTERNAL_ERROR: "服务器内部错误",
        ErrorCodes.NOT_FOUND: "资源不存在: {resource}",
        ErrorCodes.TASK_TIMEOUT: "生成超时",
        ErrorCodes.SOLVER_CIRCUIT_OPEN: "人机验证服务暂不可用",
        ErrorCodes.RATE_LIMITED: "请求过于频繁，请稍后重试",
        ErrorCodes.IDEMPOTENCY_KEY_EXISTS: "幂等 Key 冲突: {key}",
    },
    "en": {
        ErrorCodes.PROVIDER_DOWN: "Provider {provider} is temporarily unavailable",
        ErrorCodes.PROVIDER_OUT_OF_CREDITS: "Provider {provider} out of credits",
        ErrorCodes.QUEUE_FULL: "System busy, please retry later",
        ErrorCodes.UNAUTHORIZED: "Unauthorized",
        ErrorCodes.INVALID_MODEL: "Unknown model: {model}",
        ErrorCodes.INVALID_PROMPT: "Invalid prompt",
        ErrorCodes.INVALID_RATIO: "Invalid aspect ratio: {ratio}",
        ErrorCodes.BAD_REQUEST: "Bad request: {detail}",
        ErrorCodes.INTERNAL_ERROR: "Internal server error",
        ErrorCodes.NOT_FOUND: "Resource not found: {resource}",
        ErrorCodes.TASK_TIMEOUT: "Task timeout",
        ErrorCodes.SOLVER_CIRCUIT_OPEN: "Captcha solver unavailable",
        ErrorCodes.RATE_LIMITED: "Rate limited, please retry later",
        ErrorCodes.IDEMPOTENCY_KEY_EXISTS: "Idempotency key conflict: {key}",
    },
}

# 旧版错误码兼容映射
_LEGACY_CODE_MAP: dict[str, str] = {
    "QUEUE_FULL": ErrorCodes.QUEUE_FULL,
    "RATE_LIMITED": ErrorCodes.RATE_LIMITED,
    "INVALID_MODEL": ErrorCodes.INVALID_MODEL,
    "INVALID_PROMPT": ErrorCodes.INVALID_PROMPT,
    "INVALID_RATIO": ErrorCodes.INVALID_RATIO,
    "PROVIDER_DOWN": ErrorCodes.PROVIDER_DOWN,
    "SOLVER_CIRCUIT_OPEN": ErrorCodes.SOLVER_CIRCUIT_OPEN,
    "TASK_TIMEOUT": ErrorCodes.TASK_TIMEOUT,
    "PROVIDER_OUT_OF_CREDITS": ErrorCodes.PROVIDER_OUT_OF_CREDITS,
    "NOT_FOUND": ErrorCodes.NOT_FOUND,
    "UNAUTHORIZED": ErrorCodes.UNAUTHORIZED,
    "IDEMPOTENCY_KEY_EXISTS": ErrorCodes.IDEMPOTENCY_KEY_EXISTS,
    "BAD_REQUEST": ErrorCodes.BAD_REQUEST,
    "INTERNAL_ERROR": ErrorCodes.INTERNAL_ERROR,
}


def get_error_message(code: str, lang: str = "zh", **kwargs: str) -> str:
    """获取多语言错误消息，支持动态参数插值。

    按 lang → zh → 硬编码回退 顺序查找。
    """
    messages = ERROR_MESSAGES.get(lang, ERROR_MESSAGES.get("zh", {}))
    template = messages.get(code, "")
    if template:
        try:
            return template.format(**kwargs)
        except KeyError:
            return template
    return code  # 回退：返回错误码本身


class AppError(Exception):
    """统一的应用错误。"""

    def __init__(
        self,
        code: str,
        message: str,
        status_code: int = 400,
        details: dict | None = None,
    ):
        # 兼容旧版错误码（如 "QUEUE_FULL" → "SYS.002"）
        self.code = _LEGACY_CODE_MAP.get(code, code)
        self.message = message
        self.status_code = status_code
        self.details = details or {}
        super().__init__(self.message)


def error_response(
    code: str,
    message: str,
    status_code: int = 400,
    details: dict | None = None,
) -> JSONResponse:
    """统一错误响应格式。"""
    # 兼容旧版错误码
    mapped_code = _LEGACY_CODE_MAP.get(code, code)
    return JSONResponse(
        status_code=status_code,
        content={
            "error": {
                "code": mapped_code,
                "message": message,
                "details": details or {},
            }
        },
    )


# 状态码 → 错误码映射（供 main.py 异常处理器使用）
STATUS_CODE_ERROR_MAP: dict[int, str] = {
    400: ErrorCodes.BAD_REQUEST,
    401: ErrorCodes.UNAUTHORIZED,
    403: ErrorCodes.UNAUTHORIZED,
    404: ErrorCodes.NOT_FOUND,
    408: ErrorCodes.TASK_TIMEOUT,
    409: ErrorCodes.IDEMPOTENCY_KEY_EXISTS,
    413: ErrorCodes.BAD_REQUEST,
    422: ErrorCodes.BAD_REQUEST,
    429: ErrorCodes.RATE_LIMITED,
    500: ErrorCodes.INTERNAL_ERROR,
    503: ErrorCodes.PROVIDER_DOWN,
}
```

- [ ] **步骤 3：更新 main.py 使用新错误码**

修改 `main.py` 中 `starlette_http_exception_handler` 使用 `STATUS_CODE_ERROR_MAP`：

```python
from .errors import AppError, ErrorCodes, error_response, STATUS_CODE_ERROR_MAP
```

替换 `_code_map` 使用 `STATUS_CODE_ERROR_MAP`。

- [ ] **步骤 4：运行测试**

运行：`pytest tests/test_errors.py tests/test_retry_policy.py -v`
预期：ALL PASS

- [ ] **步骤 5：Commit**

```bash
git add api/errors.py api/main.py tests/test_errors.py
git commit -m "feat: Q-02 错误码分层体系 — CATEGORY.NNN 格式 + 多语言支持

- 分层错误码：AUTH/VALIDATION/PROVIDER/SYSTEM/RATE_LIMIT
- 格式统一：CATEGORY.NNN
- 多语言错误消息（zh/en），支持动态参数插值
- 兼容旧版错误码字符串
- STATUS_CODE_ERROR_MAP 供异常处理器复用"
```

---

### 任务 4：同步 deploy + E2E 验收

**文件：**
- 同步：`deploy/api/` ← `api/`

- [ ] **步骤 1：同步 deploy/api**

```bash
python scripts/sync_deploy.py sync
python scripts/sync_deploy.py check  # 应输出 OK
```

- [ ] **步骤 2：运行全量测试**

```bash
pytest tests/ -v --timeout=30
```
预期：ALL PASS

- [ ] **步骤 3：E2E 验收**

```bash
python scripts/e2e_validate.py --mode mock --api-port 8101 --concurrency 10
```
预期：全部 PASS

- [ ] **步骤 4：Commit deploy 同步**

```bash
git add deploy/api/
git commit -m "chore: sync deploy/api with api/ after F-04/F-05/Q-02"
```

---

### 任务 5：推送 + 发行版

**文件：**
- 修改：`deploy/.env.example`（更新说明）
- 修改：`deploy/README.deploy.md`（更新版本）

- [ ] **步骤 1：推送代码**

```bash
git push origin main
```

- [ ] **步骤 2：创建 GitHub Release**

```bash
gh release create v2.3.0 --title "v2.3.0 智能重试 + 缓存预热 + 错误码分层" --notes "...
```

- [ ] **步骤 3：部署到服务器**

SSH 到服务器 → docker compose build/pull → restart
